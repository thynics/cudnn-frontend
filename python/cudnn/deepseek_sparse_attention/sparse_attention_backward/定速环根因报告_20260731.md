CSV 锚点已复核 (MATH_STORE starts 103.968/110.944/117.792, S_ISSUE 间距 6.624/6.976, store-to-store 6.848),与解剖数据一致。以下为最终报告全文。

# 2-CTA DSA backward 定速环终审报告 (2026-07-31)

材料: vm5probe_run2 稳态窗 i=14–17 + i=1–3, span_means_full_run.csv (n=256), v12/final 源码, 三 lane 解剖 (cycle/math/pipe) + 六提案红队判决。换算恒等式 (台账已验): **e2e(ms) = 1.771 × period(µs)**; 现值 release period ≈ 6.55 (11.594ms), trace 口径簇沿均值 6.912, 单簇最坏 7.12。

---

## 1. 根因判词

### 1.1 定速环 = 单 period 零松弛闭环 (k=1, 非 2-period)

以 t=16→17, cta0 为例, 总长 6.848 ≈ period; 全部边贴合 ±0.03–0.13µs (CSV 复核通过):

| # | 边 | 时间戳 | 时长 | 锚定证据 |
|---|---|---|---|---|
| 1 | dVdK(t-1) 全排空 = pds 释放 → MATH_STORE(t) 开启 | G9(15)end 110.976 → ST_s(16) 110.944 | **−0.032** | 6/6 边全贴 (103.968/110.944/117.792 vs 104.000/110.976/117.824, 双 CTA); pds **num_stages=1** (v12:12413) |
| 2 | STORE (P+dS stmatrix 发布) | 110.944→112.160 | 1.216 | 八警组 lockstep ≤0.032 (簇级单事件门) |
| 3 | count-512 arrive 尾 / PD close | →112.608 | 0.448 | |
| 4 | W18 唤醒 0.128 + ROUTE_P/dS 双 DSM push + pds commit | →113.024 | 0.416 | vm5probe.py:13779–13882 |
| 5 | leader pds wait 通过 → WAIT_dQ 113.056 → dQ_ISSUE → G0 | →113.408 | 0.384 | WAIT_dQ 贴 ROUTE_dS end +0.032 (门=landing 链非 commit) |
| 6 | MAT_ACQ(q0)≡G0 (round 信用/kdq 槽复用) → chunk0 TMA 暴露 | →114.336 | 0.928 | ACQ(16,q0) 113.376 vs G0 113.408 |
| 7 | 8-pass 排空按供应率步进 0.43/pass (2 槽, L=0.61–0.96) | →117.824 | 3.424 | 8/8 发射贴 MAT_WAIT end +0.032–0.064 |
| — | G9(16) 117.824 → MATH_STORE(17) 117.792 —— **闭合** | | Σ6.848 | |

**份额**: dVdK 供应+排空 4.35µs (63%) / math STORE+arrive 1.66 (24%) / relay+commit+dQ 0.83 (12%)。
非绑定支路 (全带正松弛): S/dP 数据边 +0.45~1.06, softmax→acquire +1.06~1.44, ROUTE_K +0.61, LOAD_K +3.6; S(t+2)←dVdK(t) 的 2-period 链存在但消费端 +0.8~1.06 松弛, 不定速。

### 1.2 v16 null 的定量解释 (硬约束已复现)

- **EAGER 前提证伪**: k_eff 早就是 2 —— fill 间距 0.43–0.50 < TMA 单 chunk L=0.61–0.96 (MAT_WAIT−MAT_ACQ 实测); commit 滞后实测仅 0.032–0.192 (24 边, 均值 0.086)。EAGER 只挪提交时刻, 不加槽不缩 L, 可回收 ≤0.1µs/tile。
- **HOIST 窗真但打不中**: 可上提窗 0.256–0.416 实测存在, 但目标是环上 ≤0.29µs 的边, 且 math 帽吸收论被否定 (math 路径 5.2–6.1 vs 管线 6.85, 需先缩 0.75–1.4 才触帽); 吸收者未定位。
- **合并判决**: 两腿合计瞄准 ≤0.4µs 的边, 环的质量在供应率 (4.35) 与 store/route 段 (2.1) ⇒ 11.645 vs 11.594 (+0.4%) = 噪声内 null。五连 standalone≈0 同机制: 每刀都砍在被 2-ahead 供应与 landing 门已隐藏的延迟跳上。

### 1.3 math 5.3 与环下界 5.2–5.3: **数值重合, 不是同一堵墙**

- MATH_PD 5.09–5.3 的成分: **1.06–1.44 是 pds producer_acquire 协议等待**, 非 math; 纯 math 忙 = span 内 3.9–4.4 + span 外 T2R+fence 1.28–1.63 ≈ **5.5±0.3 (trace) / 5.1–5.7 (release, 扣 ~0.36 trace 税)**。
- 环供应下界 5.2–5.3 (两把刀: 任意环深/粒度在 +16KB 内) 独立成立。
- **两堵 ~5.2–5.7 的墙被单 stage pds 串联成 6.85 的环**。单修任一堵, period 钉在另一堵; 追平 baseline 4.71 必须三线同动: ①拆 pds 锚点边 ②删/压供应环 ③压 math 链。
- 主席预检纠偏: "math 105.3→107.1 空等 S(16)" **证伪** —— S(16)/dP(16) 完成沿 104.672/105.024 (PROBE_DONE k0/k1), math 105.472 才到达等待点; 该 1.6µs 是 T2R+双 fence 执行段, 含 tmem 端口争用 0.7–1.3 (i=2 空管 0.608 vs 稳态 1.632, 资源项非依赖边)。

---

## 2. 方案排序 (预测收益 × 置信度 / 成本)

### 幸存 (3 案, 全部 ALIVE_MODIFIED 修正版)

**#1 锚点边打断家族: v18a split-publish (轻) → v17d2-min (重)** — designer: math-attack P2 / cycle P2 合流
- 打断唯一零松弛锚点边 [1] (store(t)←G9(t-1)): pds 拆 pds_P (单 stage 不动) + pds_dS, dS-early/P-late; 或 local 半块对 ×2 + pds_local stages=2。字节预算已闭合: 死字段 ds_xchg 4,096 + p_xchg 退役 4,096 + slack 1,536 = 11,264B ≥ 8,192B 需求。
- 修正 period: **5.3–6.2** (e2e 9.4–11.0ms); 与 math 压缩合装被供应下界封底 5.2–5.5。
- 置信中高 (锚点边是全案唯一 6/6 实证零松弛边; 但 HOIST 前科提示未定位吸收者风险), 成本 1–2 rev。

**#2 v17a-math: packed f32x2 + 分相 P-then-dS (baseline 形态)** — designer: math-attack P1
- standalone **预期 period 不动** (被 pds 门钉死, v16-null 同机制自检) —— 价值 = 硬止损门 + 解锁: SOFTMAX 1.87 (=ALU 界 12–15×) → ≤1.1, math 帽 5.5→4.0–4.4, 是 #1/#3 落地后兑现的必要前提。baseline 同 4 warp@128reg 同 32 值/线程装进 4.71 是存在性证明。
- 成本 1 rev, 置信中 (packed 配对依赖 Rep4 fragment 布局)。

**#3 重构 P1 梯度归位: dV/dK 退 CG1 本地 (A=常驻 Q/dO 转置视图) + dQ 保 CG2 + 删 QDO 供应环/W18 relay** — designer: rearchitect
- 收益最大: 供应环 4.35µs (63%) 整段消失。修正 period: 检查点 A (只删环) **5.2–5.7** (math 帽裸露); 检查点 B (叠 math 重写) **4.7–5.2, 中心 ~4.9** (e2e 8.3–9.1ms, 追平带)。红队修正对其有利: dQ B 本就是 own-half ds_image (v12:11393, 13106–13110), "SMEM 超限回退场景"不存在。
- 置信中低: CG1+CG2 混流同 kernel 零先例, CTA1 需独立 commit 链, P 全像缓冲需新增 (+8KB, 删 p/ds_blocks+xchg 净 −16KB 出资), math 发布在 CG2 折叠 fragment 上是重写非移植 (v8 stmatrix lowering 病理必查 SASS)。成本 2–3 rev + 前置零 GPU 门。

### DEAD (一句话死因)

| 案 | 死因 |
|---|---|
| 提案3 寄存器再平衡 128→136/144 | math@128 SASS 已于 v11 审计干净 (61/14, 残余良性), STORE 1.18 已在 v9.3 stmatrix 修复后地板 (raw 1.08) 上, 无可回收质量; setmaxnreg warpgroup 粒度切分还踩 v15 REGSWAP 三连 STOP 雷区 |
| P1 QDO-TRANSPOSE | 前提假: 常驻是 H-own 切分, peer 半 4 gen 的源数据不在本地 SMEM; own-half 已是本地 bulk (OWN_HALF_BULK=True, v12:13601–13620) 且实测 L 与 GMEM TMA 同分布 (0.72 vs 0.73µs) —— L 是 async 队列+信令+服务环, 不是内存飞行; 零拷贝备选是 kill list 已死项 (CG2 rank 对称硬件封死) |
| P3 RELAY-RETIRE | "最后到达做 commit" 自相矛盾 (本地全像可见性认证必须 count 聚合, 物理不可裁撤); math 帽 −0.448 是虚账 (fence 已在 STORE span 内, 尾巴只剩 arrive+advance+IKET 税); 诚实上限 0.25–0.35 恰落五连 null 带 |
| 重构 P2 D-own 全常驻 | TMEM 真实需求 640>512 列 (靠静默砍 S/dP 乒乓凑账, 重耦合 v1 病理); SMEM 漏 K 双缓冲 (+64KB 后 misc 仅剩 3KB); period 账双重记账一个未列 scope 的 math 重写; 最好情形也不过 baseline 线 |
| 重构 P3 MXFP8+深度4 | SFA/SFB 按 PTX 须驻 TMEM 而 512 列全满; 深度 4 相位非法 (10 gen % 4 = 2, 静态绑定死锁); 环内一半字节 (own-half bulk + kdq gather) 不经 GMEM, "host 预量化零成本"结构性不成立; standalone 9.2–9.9ms 还烧掉唯一一次数值合同变更 |

---

## 3. 实验队列 (授权自由迭代, 按序执行)

### E1 — baseline_trace 采窗 (半天, 零改动, 三个判决共同要求)
- **fork**: 无。用已交付的 baseline_trace.py (commit 60d0d70, 评审 SHIP_AS_IS)。
- **改**: `_interface_sm100.py` L13 换 import 指向 trace 版, fresh process, 只取 raw decoded_results。
- **跑**: run-on-haifa drop-box (托管会话禁 ssh): job dir + run.sh 执行 baseline smoke ×1 + trace 解码, 等 result.txt。
- **看**: baseline 4.71 period 内 (i) math P-腿串行占比 (单/双 hop t2r 形态); (ii) DKV_SLOT (payload≥2) / T2R_BAR 稳态是否 ≈0 —— 判 baseline 是否 drain-bound。
- **GO/STOP**: math-leg 串行 <4.0 → math 压缩标的真实, E2 目标定 ≤1.1; baseline 实为 drain-bound ~4.6 → 追平线即物理地板, 全部下游预期按 4.6–4.7 封底重标; **本实验无 STOP, 只定标**。

### E2 — v17a-math: math 形态重写止损门 (fork v12, 1 rev)
- **fork**: dsa_bwd_sm100_2cta_v12.py → v17a。只换 math 循环: packed f32x2 (对表 dsa_bwd_sm100.py:1825–1874) + 分相 P-then-dS 计算 + 两段发布顺序; **pds 单门与 count-128 不动**。
- **跑**: haifa smoke ×3 vs final 11.594ms + vm6probe 窗 i=14–17; math warpgroup SASS 过门 (STSM>0, STL/LDL≈0 —— v8 lowering 病理复检)。
- **看**: ① MATH_SOFTMAX 1.87→**≤1.1**; ② 分相后 dS 相 store span (≤0.85 vs >0.85); ③ period 不动 (环模型自检, 动了=模型错)。
- **GO/STOP**: ①②双过 → E3 立项且重构 P1 检查点 B 的 math 腿有据; ②>0.85 → store 是固定开销主导, 锚点边打断收益上限 <0.4, E3 降权、改查 store 本体; ①失败 (packed 不兑现) → math 地板 5.5 成立, 追平路径只剩重构 P1 且预期降级。

### E3 — v18a split-publish: 打断锚点边 (fork v12, 1–2 rev, E2 门后)
- **fork**: v12 → v18a。(i) pds 拆 pds_P (原样) + pds_dS 两条单 stage (+2 mbars); (ii) dS store 前门改 max(softmax(t), dq_done(t-1), ROUTE_dS(t-1) landing) —— 相位机器均已存在; (iii) ds landing ×2 (+4,096B, 由退役 ds_xchg 出资, 预算闭合); (iv) W18 双 ready (ds_ready 早推 ROUTE_dS+commit, p_ready 晚推); (v) **v7 发射序局部回退** (dQ(t) 提到 S/dP(t+1) 前); 硬门: SharedStorageV2 ≤232,448 编译断言 + G2 寄存器门。
- **跑**: haifa smoke ×3 + vm6probe i=14–17。
- **看** (判决签名): (a) MATH_STORE(t).start 与 G9(t-1) **解耦** (今 6/6 边 ±0.032 → 预测贴 SOFTMAX end +0.1, 八警组 lockstep 解体为 0.3–0.7 错峰); (b) softmax_end→store_start 等待 1.44→≤0.2; (c) S_ISSUE 间距 ≤6.2; 负控: dVdK per-pass 节拍保持 0.43–0.50 (供应环未动)。
- **GO/STOP**: (a)(b)(c) 全兑现 → 合入 E2 math 形态, 冲 5.2–5.5; (a)(b) 兑现但 period 停 5.9–6.0 → v7 回退未除净, 抓 dQ 相位; 停 5.5–5.7 → math 帽接管 (预期内), E2 成果立即合入; **净降 <0.3 → 停, 上 W18 P 段/gate-A/gate-B 三探针定位吸收者** (勿重复 v16 无探针合并的教训)。

**并行零 GPU 项** (不占 rev): 重构 P1 的 [0b] 四项宿主 assert (CG1 转置视图描述符 / p_image swizzle 恒等 / CG1+CG2 混建可编译 / CTA1 独立 commit 链危险图) —— E1–E3 期间完成, 结果决定重构 P1 是否在 E3 后立即接棒为主战场。Rider: 若任一 rev 顺带跑 v16, 加三刻度 (leader WAIT_dQ 前洞 1.15→?; dQ_ISSUE 相位; W19 服务延迟) 关闭五连 null 吸收者悬案。

---

## 4. 诚实的 period 可达区间与追平概率

| 阶梯 | period (µs) | e2e (=1.771×p) | 置信 |
|---|---|---|---|
| 现状 (release) | 6.55 | 11.594ms | — |
| E3 v18a 单独 | 5.7–6.2 | 10.1–11.0ms | ~65% |
| E3+E2 合装 | 5.2–5.7 | 9.2–10.1ms | ~50% (供应下界 5.2–5.3 封底, **此路径到此为止**) |
| 重构 P1 检查点 A (删供应环) | 5.2–5.7 | 9.2–10.1ms | ~40% (math 帽裸露为判据) |
| 重构 P1 检查点 B (+math 重写) | **4.7–5.2, 中心 ~4.9** | 8.3–9.1ms | ~25–30% |
| 追平 4.6–4.62 (≤8.14ms) | 条件尾部 | ≤8.1ms | **~10–15%** |

追平 4.6 需要四个条件全部成立: (i) E1 证明 baseline 发射侧确有 ~0.5µs 可回收 (若 baseline 本身 drain-bound 4.6, 追平线即物理地板, 超越概率 <5%); (ii) CG1+CG2 混流可编译且 CTA1 commit 链无危险 ([0b] 门); (iii) math 重写在 CG2 折叠 fragment 上落到 4.0–4.4 且 SASS 门过; (iv) 无未建模第三墙 (检查点 A 的 >6.0 止损线负责暴露)。

**结构性结论**: 6.55→~5.3 (−19%) 是工程确定性较高的区间 (两条独立路径都指向它); 5.3→4.9 只有重构 P1 一条路; 4.9→4.6 是条件尾部, E1 半天内即可裁决其是否存在——**先跑 E1**。