# SESSION HANDOFF — 2026-08-03（v3.1/v3.2 双轨实现战役）

新 session 起点文件。配合台账 memory（dsa-bwd-2cta-v2-design.md）与本目录各设计文档使用。
接手时先读：本文 → V31_SURGERY_SPEC.md → T3细粒度全f32转置设计_20260803.md → T32_BUILD_ADDENDUM.md → V32_BUILD_LOG.md。

## 0. 使命与铁律（不可挑战）

- 战场：`python/cudnn/deepseek_sparse_attention/sparse_attention_backward/`，B200，H128/D512/S4096/topk2048。
- 恒等式：e2e(ms) = 1.771 × period(µs)。锚点：baseline 8.0-8.13ms（period ~4.59-4.71µs）；v17a = 11.51ms（6.50µs，历史最优 CG2）；v18a = v17a+3.8%（E3 实验，correctness 硬件背书，v3.1 的基座）。
- **CG2 铁律**：全 GEMM cta_group::2，退守 CG1 无意义（va1 4.94µs 仅校准点）。
- **精度铁律（用户立法+口径细化）**：判据 = 误差类别不劣，三分类：混合精度（f16 累加/FP8）死刑｜bit 级等价（纯重排）无条件合法｜近似替换（全 f32、须实测证明不劣、逐档裁定，先例：M2 FFMA deg6 exp 获准、deg5 不合格）。
- **用户最新裁决（本 session 尾）：v3.1 冻结（"没有希望了"），全力 all-in v3.2。**

## 1. v3.1（v19-MAXFUSE）终局——已冻结

文件 `dsa_bwd_sm100_2cta_v31.py`（v18a 克隆基座 + 旗标手术；全关=v18a 逐位同构）。
旗标：`DSA_V19_CHASE` / `DSA_V19_DBUF`（模块级 env 读取，但**远端 harness 不吃 env**——三层结构本地薄壳→ssh 远端→容器；旗标二分只能改默认值+commit）。
规格：`V31_SURGERY_SPEC.md`（数据流向基准：DSM (src,dst@peer) 参数序、owns_n ±n_owner*4096B 偏置、
别名竞态解法、parity 双份、读释放协议——审计对照基准，精确可靠）。

### rev 史（全部已推送 dev/longcheng）
| rev | commit | 结果 | 教训 |
|---|---|---|---|
| rev0 | 771b9d7 | UnboundLocalError dq_epi_tile | #9：host 构造代码跑在 __call__ 内同受 staged 分支规则，裸 if 赋值不逃逸；getattr 条件也要 const_expr |
| rev1 | 3a8939e | 编译过；dense 死锁 | #12：dq_done 每 QUERY tile 只 commit 一次（TAIL-only）——per-kv-tile 门要专用管线 |
| rev2 | 0b3bcbd | 仍死锁 | 新建 dq_land 专用管线（leader 每 tile dQ 后 commit，tcgen05 跟踪）但 consumer 计数错 |
| bisect-A/B | 6ed925c/f759332 | A=SMEM 断言拦截（DBUF-only 结构性超帽 238,592，CHASE 的 8,192 出资 DBUF）；B=CHASE-only 仍死锁 | 断言体系工作正常；死点在 CHASE 也在 DBUF |
| rev3 | 8b40ec9 | 换新错：装饰器 | #10：**簇管线 consumer 计数必须 ×atom_thr_size(2)**——两 CTA 各 release 一次、空 mbar 每 tile 翻两次、producer 相位失步=确定性死锁（pipe_chase+pipe_dq_land 双中招，审计面板与硬件二分交叉定罪）；#11：跨 CTA 裸 mbar 门照 relay_mbars 先例（init 2+双侧 arrive(Int32(0))+_mbarrier_wait_acquire_cluster），fill3 曾是 CTA-local=静默竞态 |
| rev4 | 3bf45b5 | **correctness 4/4 PASS**；性能 13.35ms | #13：@cute.jit 必须最内层且不可叠 |
| rev5 | 48b69e3 | 4/4；12.77ms | fill3 等待遮蔽窗扩容（S1,S2,dP1,dP2 32 原子压在等待前——发射 ns/执行 µs 的窗口语义）+ FATAL-2 归位（relay+1 落地等待下沉到 grad 块第 5 位，合法性：ROUTE_dS 源=ds_xchg 暂存 + math 像 WAR 由 pds 深度-2 覆盖） |
| rev6 | dc8d6b0 | 4/4；13.02ms（归一化略劣于 rev5） | s_done commit 前置（S3 后）未能兑现——盲修停止 |

### 终态与未决
- **性能对 v17a 净回归 ~1.3-1.8ms，v19 批准案中心 5.5µs 未兑现（实测 7.2-7.5µs）**。
- 三轴审计（DSL 约束/协议死锁/数值等价）全过，数值等价零致命。
- rev6 trace 已取回本地：`/Users/longcheng/runner/20260803T141355Z_21af/`（2cta+baseline 的
  iket.decoded_results.json）——**未分析**。复活 v3.1 的第一步就是解剖它。
- 剩余亏损嫌疑（未证）：chase 8KB 片 fill 率（128B 行×2 请求）、dkv slot0 commit 延后两拍（drain 起点右移）、
  pair 结构性排队、12→10 gen 供应环相位变化。
- 审计 rev0 钉桩项（未做）：read-release wait_group.read 对 shared::cluster 方向的 SASS 验证；
  stats 迁 offset0 后的消费路径对齐断言；dS 别名窗口探针（IKET）；批准案 gate 文档同步（DBUF 独立 bisect 承诺作废）。

## 2. v3.2（T3-64）——ALL-IN 主线

### 设计血统（全部定案）
- 转置取向 Sᵀ=K·Qᵀ（头在 N 轴）+ 细粒度全 f32 + 倒推法三元组（轴对齐/粒度/编排）。
- 主形态锁定：**h_c=64 / D_c=128 / dKV 双槽 / 全 f32**（8-agent 设计工作流 + 双审计 + 批评家；
  细档 h16 双重死刑（N16 发射税→MMA 忙线 ~13µs + 死锁证明被推翻）；中档 h32 被支配；
  D_c=256 单槽相位拆分窗算裁决不升格（期望 −0.1µs，登记条件复议期权））。
- dq_b 修复终稿 = **dS 双像**（rank 对称双子像单区，own 半二次 stmatrix + peer 半 8KB DSM，
  修复后第三审双轴通过）——见 `T32_BUILD_ADDENDUM.md`（含勘误 #1：mb_dqb_free[h] 必须覆盖两个
  D-round 消费者，正确性级）。
- 预期 period 5.0-6.0 中心 ~5.5；两颗前置裁决：(A) MMA 原子单价 microbench（≥0.045 全案归档；
  repo 里已出现 `benchmark/dsa/mma_atom_price.py`——**平行 session 的产物**，可直接用）；
  (B) chase 8KB fill 率实测。
- 战略判词（供参考，用户已用行动覆盖）：与 v19 中心同为 5.5，赌的是底盘期权（供应墙 2.3-2.9、
  双墙手术唯一全额兑现底盘、Rubin 直通）。

### 建造状态（本 session 结束时刻）
- 文件 `dsa_bwd_sm100_2cta_v32.py`（v17a 克隆骨架增量转换，~15,183 行，**未提交**）。
- 建造方式：后台 agent + 硬化协议（心跳 `V32_BUILD_LOG.md` 每段一行、单编辑 ≤250 行、每段 py_compile、
  禁止全量读）。**注意：该 agent 隶属本 session，session 关闭即停**——新 session 接手方式见 §4。
- 进度（22:38 终态）：**12/12 全部完成**（[1]常量/TMEM 映射 [2]SMEM 231,424 账 [3]host 布局/TMA
  [4]gather/chase [5]原型视图/管线 [6]五 GEMM 发射 helpers [7]math 三发布 [8]W17 12-gen 供应环
  [9]簇门中继（mb_dqb 勘误#2 武装、st.async 标 TODO 走 8KB DSM 预案） [10]drain ×8/bundle +
  dQ epilogue（保 v17a 取向、staging 迁 round bufs） [11]leader dr-major 排程 [12]自审尾注 +
  休眠 helper 清理（−462 行）+ IKET 26≤29）。py_compile 全过。V32-TODO ≥9 个（grep 可得）。
  建造 agent 在交付终报前被 session 关停——**文件本体完整**，接手直接从 §2 流水线第 1 步开始。
- 合同强制项（建成后必查）：字面 `class FlashAttentionDSABackwardSm100TwoCTAV2`、恰一个 IKET
  provenance mark、IKET 静态 span 名 ≤29、**管线要求 impl 已 commit 才能跑**（prepare 有
  git ls-files + diff --quiet 检查）。

### v3.2 接手后的流水线（已定案）
1. 结构门：类名/provenance/IKET 清点/py_compile。
2. 三轴对抗审计（复制 v31 面板任务书，**加四个点名攻击向量**：簇管线 consumer 计数 ×atom_thr_size、
   跨 CTA 裸门 relay 三件套、done-commit 节奏 vs math 关键路径（发射 ns/执行 µs）、
   发射流阻塞窗算术）+ 9 个 V32-TODO 逐个裁决。
3. 修复 → commit → push → `--impl v32 --mode validation` → 迭代（预期编译门/死锁若干轮，v3.1 先例
   完整可查）→ `--mode all` 拿性能。

## 3. 基建速查

- **drop-box**：`~/.claude/skills/run-on-haifa/scripts/submit.sh <<'EOF' ... EOF`，等 `<jobdir>/result.txt`；
  产物回传：目标文件 cp 进 job 目录（用 `JOB=$(ls -td /home/longcheng/runner/*/ | head -1)` 定位自身，
  $0/dirname 不可用）；result.txt 是 tail，大文件勿走 stdout。
- **管线**：`./benchmark/dsa/run_b200_pipeline.sh --impl vXX --mode validation|all`（all 含 correctness+
  benchmark+trace）；投递前 `git stash push benchmark/dsa/README.md || true; git pull --ff-only`；
  轻量结果落 haifa `~/cudnn-frontend/.dsa_b200_results/<run_id>/`（含 traces/{2cta,baseline}/iket/pid_*/
  iket.decoded_results.json）；重档案在 computelab `/home/scratch.longcheng_gpu/dsa-b200-harness-results/`
  （haifa 不可见）。
- **git**：分支 dev/longcheng（github.com:thynics/cudnn-frontend）。**注意有平行 session 在同分支推送**
  （V4 pool rotation、mma_atom_price.py 等）——commit 前 pull，勿覆盖他人工作。
- DSL 约束清单：老 8 条（mod-8 regs、make_ptr/toint、staged 禁 raise、const_expr 分支、type-is-int、
  裸分支预初始化、张量不作运行时实参、trace-prepare 数静态名含 else 分支）+ 本 session 新增 #9-#13（见 §1 表）。
  另：staged 内禁嵌套 def/生成器表达式；struct 断言上界式+实际值回显。

## 4. 新 session 立即行动清单

1. 读本文 + `V32_BUILD_LOG.md` + `grep -n "V32-TODO" dsa_bwd_sm100_2cta_v32.py`。
2. 检查 v32 建造是否被 session 关闭截断：若 [10]/[12] 缺失，起新 agent 续建（任务书模板=本 session 的
   硬化协议：心跳/限幅/py_compile/坐标图；把 §2 的四个攻击向量与 T32_BUILD_ADDENDUM 勘误一并注入），
   或人工补两段（[10] drain 接线可参照 v17a `_drain_dkv_v8` + `_store_dq_epi_tma_v12` 复用模式）。
3. 走 §2 流水线（结构门→审计→validation→all）。
4. 性能不达时的既定武器：trace 解剖（本战役标准流程，rev6 的 v31 trace 亦在本地可作对照）、
   mma_atom_price.py（裁决 A）、chase fill 探针（裁决 B）。
5. 台账同步：memory `dsa-bwd-2cta-v2-design.md`（主账本）+ `no-precision-regression-iron-rule.md`。

## 5. 本 session 实测数字总表

| 实现 | correctness | latency | period | vs baseline |
|---|---|---|---|---|
| baseline（同批） | — | 7.99-8.13ms | ~4.59µs | 1.00× |
| v17a（锚点，历史） | 4/4 | 11.51ms | 6.50µs | 1.42× |
| v31 rev4 | 4/4 | 13.35ms | 7.54µs | 1.64× |
| v31 rev5 | 4/4 | 12.77ms | 7.21µs | 1.57× |
| v31 rev6（终，冻结） | 4/4 | 13.02ms | 7.35µs | 1.63× |
| v32 | 未跑（建造中） | — | 目标 5.0-6.0µs 中心 5.5 | — |
