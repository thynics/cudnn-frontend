# V6 build log (fork of v5 @ bd22106; spec V6_FAT_ATOM_SPEC.md)
[atom-price·协调者] B200 实测原子价曲线（mma_atom_price.py，N 扫描，fixed≡rotate）：N32=11.2ns/条(5.61/n16)、N64=13.4(3.35)、N128=17.2(2.15)、N256=34.5(2.16，饱和)。判决：①固定税假说成立，h64=1.68×效率、h128=2.61×、N128 摊平；②累加器复用不串行；③孤立价 11-35ns vs 内核实测入队价 ~200ns = 15×差距，且内核里 G1 小原子与 dVdK 大原子同价 ⇒ 内核 0.2µs 为形状无关的每条前端/仲裁成本（嫌疑：UMMA 前端、TMEM 端口与 math/reduce T2R 争抢）⇒ v6 减条数收益直接兑现；同时标记 v7 级矿脉（200ns→孤立价 = MMA 面 5×+ 余量）。release 预测更新：h64 → ~14µs/64kv ≈ 25ms 档。
[z0] 阅读期完成：spec/V5_BUILD_LOG 全 130 行/HANDOFF §1§3/V32 [13][14]/V5_TILING4 全读；fork 已 cmp 验证 v5==v6 逐字节；教训表 #1-#18 已入脑（#14 三态返回、#16 zip 相位、#17 动态 cosize 禁宿主控制流）。
[z1] 侦察-常量层：SUB_TILES=4@11743, SUB_TILE_BOX=16@11745（语义三重合体：h16 N-half 箱宽=TMEM 槽宽=头解码模数），SCORE_MMA_N=32@11737, SCORE_B_STAGES=16@11775, TMEM 映射@11889-11899（S 32/48, dP 64/80, dkv base 96, BUDGET 352）；score_tiler 由 SCORE_MMA_N 钩子驱动@512, score_b_layout=make_smem_layout_b(...,16)@617。
[z2] 侦察-score 环：helper _issue_score_pass_v5@15088-15214，B 索引 (sub_tile%2)*8+piece@15164/15190，k_blocks=2（mode[2]），(s,dp) 每 pass 单 stage 对=4 commit/bundle ✓ 与 spec 描述吻合。
[z3] 核实中·zone2 关键疑点（未决，正在取证）：strip 几何 = [h16×D512]/stage（STATIONARY_TILE_H=16@11805，2 stage×8192 el/张量）。N64 CG2 的 per-CTA B N-half = h32×D512 = 16,384 el = 两个 strip stage——与 spec 前提"每 h64 子块恰一 strip stage"疑似冲突；且 n32 原子跨 stage 的 n-atom 字节步距 (512,7680,512)el 非均匀，单 MMA 描述符疑似不可表达。正在读 W17 strip gen（14671-14770）与 leader 调用点定罪/翻案。
[z4] 核实-zone3 前提【判定：成立】：P/dS 槽本就 chunk-major [own-kv64×h64]——PDS_BLOCK_ELEMENTS=4096/8192B@11864-11865、storage p_slab/ds_slab 8192 el 双 chunk 子像注释@12177-12188、grads pair helper 按 chunk 像整体消费（4 K16 块 kb∈{0,2,1,3}）@15243-15259。v6 sub_tile(h64)==chunk==v5.1 pair ⇒ grads/驱逐/relay 面零触碰判定成立（若 zone2 活的话）。zone4 轮转（rotation point sub_tile==2@14503 → 改 ==1）与 zone5 payload（15139 处 SUB_TILES 常量自适应）均机械可行。
[z5] 核实-zone1 SUB_TILE_BOX 语义【触发 spec 的"别的语义"分支，记录】：全部 5 处用法均为"头空间 h16 N-half 箱宽"而非"TMEM 列半宽"——(a) head_base j*BOX@13987-13990（头偏移）；(b) 头解码模数 n%BOX + (n//BOX)*SUB_TILE_H@14018-14028×2，其中乘数 SUB_TILE_H(32) 与 rank 步距 32 是 v5 巧合同值，SUB_TILE_H→64 后此解码必错（v6 需乘数→HEAD_CHUNK_GROUP(32)，即 head=t6*64+n 自然映射）；(c) 发布域 (16,2) 列箱@12899-12925；(d) 尾部 t_rs 断言@15724-15756。TMEM 槽宽 16 是独立字面量（TMEM_S1_OFFSET=48 等@11889-11899），不经 SUB_TILE_BOX。此项单独可解（逐点改+乘数换钩），非致命；致命项见 [z6]。
[z6] 【STOP 主罪·zone2 结构性做不通】spec 前提"每 h64 子块恰一 strip stage"对代码为假，肥原子 B 操作数在冻结 SMEM 字节下无合法描述符。证据链：
  (1) strip stage = [h16×D512] = 8,192 el：STATIONARY_TILE_H=16@11805、strip_stage_bytes=16,384B@14684-14688、stationary_q/do 各 16,384 el（2 stage）@12155-12162。
  (2) N64 CG2 的 per-CTA B N-half = h32×D512 = 16,384 el = 恰好两个 strip stage。sub_tile t6 需 h16-tile {4t6+2r, 4t6+2r+1} = strip gen {2t6, 2t6+1} → stage {0,1} 双占（gmem 公式 4*(g//2)+(g%2)+2r@14740-14744）。"恰一 stage"不成立，任何 h64 pass 必然双 stage。
  (3) 描述符代数：n32 的 4 个 (8,64) SW128 核矩阵落位元素偏移 {p*1024, p*1024+512, 8192+p*1024, 8192+p*1024+512}，N 向间距 (512,7680,512) 非均匀 ⇒ 单条 K-major UMMA 描述符（单 SBO）不可表达；auto make_smem_layout_b(N64, 8-stage) 的 stage 字节序（[n32×k64] 连续 2048 el 块）≠ strip 物理序（同 h16 tile 的 k64 块连续）⇒ 直接绑定必错字节。
  (4) 即便走 16 宽窗别名 ((8,2):(1024,0)) 保 spec 索引式 sub_tile*8+piece：每 pass 全程双占两 stage ⇒ W17 无法在 pass0 期间预填 pass1 条带（同字节复用）⇒ 每 bundle 在 score(0)→score(1) 间强插一次全量填充延迟 = 供应对 pass 流水的伪串行化（纪律明令禁止），且直接污染肥原子论题的测量。
  (5) 连带伤：W17 strip 环以 range_constexpr(SUB_TILES) 计数@14706-14708——SUB_TILES 4→2 令 strip gen 掉到 2/bundle、gmem 公式只装 h16-tile {2r,2r+1}，h 高半永不入 SMEM = correctness 必死。spec 手术区未含 W17 strip 区（zone5 反而假设"数目不变"），此依赖 spec 未见。
[z7] 修复选项（供裁决，未动刀）：
  R-A【贴 spec 字面】：手工 16 宽别名 B 窗（n-rest (2,2):(512,8192)）+ W17 环与 SUB_TILES 解耦（保 4 gen 原字节）+ 每 pass 双 strip wait/release。风险：(2,2) 非均匀 N 步距的描述符合法性 = 全新未验风险类（须先投 haifa 宿主编译探针裁决，若 DSL 拒收全案死）；且 (4) 的供应串行化气泡内生不可除——不推荐。
  R-B【贴论题】：strip 重装箱 [h16×D512]→[h32×D256]（同 16,384B 单 box、同 4 gen/bundle：gen g=(t6=g//2, dhalf=g%2)，gmem 窗 = 头 [t6*64+r*32,+32) × D[dhalf*256,+256)，h32-tile 索引 2*(g//2)+rank）+ stationary_tiler (64,16,512)→(64,32,256) + score-B = auto make_smem_layout_b(N64, (128,64,64), 8 stage) 直接绑定——字节恒等式 s*2048 = (s//4)*8192+(s%4)*2048 精确成立，描述符全程 canonical（零新风险类）；B 窗索引 = piece（8 宽末模，spec 的 16 宽式作废）或加手工别名窗保字面式；pass 内早释放 stage0（piece3 后）⇒ W17 预填与 pass 尾重叠，v5 供应重叠形态完整保住；strip 管线 4 产=4 耗（每 pass 2 wait+2 release）；MAT_ACQ 序号表 0-3 不动（zone5"数目不变"满足）。代价：W17 strip gen 块重写（~30 行，不在冻结名单但也不在 spec 手术区）+ B 窗索引合同偏离 spec 文句。推荐 R-B。
  预算复核（两案同）：score 64 条 N64 原子/bundle（2 pass × 8 piece × 2 k_block × G1G2）✓；TMEM 416≤512 ✓；SMEM 字节量全部不变 ✓；math h32 内层消费/寄存器预算不动 ✓。
[v6-STOP] 终裁：按纪律"spec 做不通就 STOP"中止建造。触发点两个：zone1 SUB_TILE_BOX"别的语义"分支（[z5]，可解非致命）+ zone2 结构性矛盾（[z6]，致命：strip h16 几何 × N64 描述符代数 × 供应重叠三者在 spec 字面下不可同时满足）。文件零改动（cmp v5==v6 已验），py_compile OK（fork 基线）。冻结区自证：一个字节未动（全文件未编辑）。建议：裁决 R-B 后复工，届时手术区顺序改为 zone1(常量+SUB_TILE_BOX 逐点) → W17 strip 重装箱（新增区，需授权）→ zone2 score 环 → zone3 math → zone4/5；若坚持 R-A 须先投 haifa 描述符探针。
[z8] 复工接令：R-B 批准（spec 修正案 #1 已读），W17 strip 区解冻为 zone1.5；haifa 探针注销，测试走协调者 ~/proxy。
[z9] 落笔前推导定案（关键代数，全部闭合）：
  (1) N64 fold 规则 = N-参数化硬件同族：col=n%(N/2)、DP-half=n//(N/2)（v5 N32/dkv N128/evict N32 三处硬件背书）⇒ v6 S 槽 32 列，16 列窗 j 的 (DP,col)→head 逐点恒等于 v5 sub_tile (c=t6,j)——**math 的 h32 切片机器 = v5 sub_tile 机器逐字保形**（slice s=2*t6+j ≡ v5 t）：头解码公式、(16,2) 发布箱、J 切片域、pds 4/bundle、寄存器 16 vals 全部零值变。
  (2) slice 布局构造：score_slice_c_layout = dq_evict MMA 的 (128,32) fragment_C fold（fragment_C 只依赖 (M,N,dtype,cg)，与操作数 major 无关；与 v5 score_c_layout 同构），4 slice 源张量 @ S/dP 基址+16s 静态偏移（v32 静态槽先例）；坐标域 (128, MATH_SLICE_H=32) 经 evict MMA partition_C。
  (3) 常量拆义：SUB_TILES=2/SUB_TILE_H=64（调度）；新 MATH_SLICES=2/MATH_SLICE_H=32/MATH_SLICE_BOX=16/MATH_SLICE_VALS=16（消费，= v5 值恒等改名）；SUB_TILE_BOX/SUB_TILE_VALS 退役。TMEM: S[32,96) dP[96,160) dkv[160,416)，BUDGET=416。
  (4) strip 重装箱账：stationary_tiler (64,16,512)→(64,32,256)（新钩子 STATIONARY_TILE_D，base=None→D_HEAD 保 dormant 类恒等）；box 16,384B 不变、expect_tx 32,768B 不变、gen 4/bundle 不变（g=(t6=g//2,dhalf=g%2)，stage=g%2，gmem h32-tile=2*(g//2)+rank、d-tile=g%2）；score-B=auto make_smem_layout_b(N64,(128,64,64),8)，窗索引=piece，字节恒等 s*2048=(s//4)*8192+(s%4)*2048。
  (5) 协议账预算：strip 4 产=4 耗（每 pass 2 wait+2 release，piece4 界早释 stage0 保预填重叠）；s/dp done 2 产=2 耗（math 于 j==0 wait、j==1 release）；pds 4=4 恒等；kres 1=1（release 移至 score(1) 后、grads(0) 前——追踪集恰 S0+S1）；round 20=20、dkv/evict/dqb 恒等。
  (6) 手术编辑策略：zone1 同时完成全文件纯改名位点（值恒等），结构改动留各自 zone；老常量即刻退役防陷阱；每 zone py_compile + grep 残留。
[z10] zone1 常量层完成：SCORE_MMA_TILER=(128,64,64)/SCORE_MMA_N=64/SUB_TILES=2/SUB_TILE_H=64@11749-11757；新 MATH_SLICES/MATH_SLICE_H/BOX/VALS=2/32/16/16（v5 值恒等改名，语义注释拆义）@11771-11774；SCORE_B_STAGES=8（auto N64 绑定字节恒等注释）@11801；STATIONARY_TILE_H=32+新 STATIONARY_TILE_D=256@11831-11833（base 钩子 None→D_HEAD@196-200，__call__ tiler 三元式@496-510，dormant 类恒等）；TMEM 重排 S[32,96) dP[96,160) dkv[160,416) BUDGET=416@11915-11926（断言带回显@738 自动生效）。全文件值恒等改名 13 BOX+4 VALS+4 乘数（V2 区限定，其余类 kernel 零触碰，rank_score_mma 仅存于 base/V0/V1A0 ✓）。发布域/坐标域改 slice 口径：score_store_domain 注释+MATH_SLICE_*@12933-12967；坐标 identity (128,32) 改经 dq_evict MMA partition_C（N64 无法切 32 宽 C tile；(128,32) CG2 fold 与操作数 major 无关，v5.2 r10 硬件背书）@13135-13153；新 score_slice_c_layout + t_score_slices/t_dp_slices 各 4 静态窗 @ 基址+16s（v32 静态槽先例）@13496-13600。py_compile OK。
[z11] zone1.5 strip 重装箱完成：gmem 双视图 tiler (H_TILE_CTA, 32, STATIONARY_TILE_D=256)@13092-13118（(h32-tile 4)×(d-tile 2) 网格）；W17 strip 环重写@14798-14900：循环 range_constexpr(2*SUB_TILES)=4（与 pass 数解耦），gen g=(t=g//2,dhalf=g%2)，gmem 索引 [None, Int32(2*(g//2))+rank, Int32(g%2)]，smem stage=g%2 原式保形，strip_stage_bytes 改 STATIONARY_TILE_H*STATIONARY_TILE_D*2=16,384B（expect_tx 值 32,768B 与 v5 恒等），LOAD_QDO payload 乘数修为 2*SUB_TILES=4（防跨 bundle 塌缩），MAT_ACQ 序号 0-3 不变；q_strip_b/do_strip_b 绑定代码零改（8-stage N64 auto 布局经 score_b_layout_staged 自动换发），字节恒等注释重写@12735-12745；pipe_strip 协议注释改早释放合同（4 产=4 耗）@13355-13368；LOAD_WARP 头注同步@14768-14775。py_compile OK。
[z12] zone2 score 发射环完成：_issue_score_pass_v5 → _issue_score_pass_v6（@15200 起）——B 窗索引 (sub_tile%2)*8+piece → piece（修正案合同，8 宽末模）；每 pass 32 条 N64 原子（8 piece × 2 k_block × G1G2），64/bundle vs v5 128 ✓；strip 协议改双 stage：头部 wait(s0)、piece==SCORE_D_PIECES//2 界早释 s0+wait(s1)（早释追踪集=D-lo gemms，UMMA 跟踪；s1 等待落 S_ISSUE 跨内注记入 docstring 供判读）、尾部释 s1，state 前进 ×2 经 #14 返回；s/dp done 2 commit/bundle（stage==t 静态）；S_ISSUE payload=issue_seq*SUB_TILES+t 自动=b*2+t ✓；S_ISSUE→dP_ISSUE 切换点原位。py_compile OK。调用点重接留 zone4。
[z13] zone3 math 消费环完成：T2R 分区块重写（4 copy/4 source 每平面，元组静态索引，v7 ping-pong 先例 ×2；coordinates 仍经 slice-0 copy partition_D）@13888-13975；主循环改 sub_tile(2)×slice_j(2) 双层（slice_j 层 2 格缩进规避 300 行整块重排，Python 合法，风格妥协记录在案）；s/dp done 改 pass 级：wait@j==0、release@j==1（32 列槽读毕才放 WAR），j==1 的 WAIT_S/WAIT_dP 空跨零宽自证；fence_view_async_tmem_load 每 slice 保留（供 softmax 用寄存器）；head_base = sub_tile*64 + j*16（v5 公式以 s=2t+j 代入逐字保形，解码 n%16+(n//16)*32 零值变）；pds acquire/commit 每 slice 4/bundle 恒等，MATH_PDS_ACQ=4i+s、chunk_payload=8i+2s（v5 编码恒等）；发布 tiles[slice_idx]、dqb tiles[slice_j]、own 条件 Int32(sub_tile)==rank；dqb_free 于 slice_idx==0。py_compile OK，score_source_pp/dp_copy_pp 等旧名残留清零。
[z14] zone4 leader 轮转完成：总序 score(0); {score(1); kres release; grads(batch0)}; grads(batch1); G5r0; G5r1; dqb-free（旋转点 sub_tile==SUB_TILES-1 const_expr@14666-14733）；调用点换 _issue_score_pass_v6 + 累加器选择 sub_tile==0（值同 %2 式）；kres release 移入环内 score(1) 后、grads(batch0) 前——tcgen05 追踪集恰 S0+S1（grads MMA 不再垫长空边），gather K(i+1) 预填重叠 batch0/batch1/G5 全尾；grads 双 batch = 冻结 pair helper 逐字调用（pair0/1 = chunk0/1，pds 双 stage 语义 = math slices {2t,2t+1} 天然配对）；strip_cons/pds_cons 注释改新合同。py_compile OK；_issue_score_pass_v5 残留清零。
[z15] zone5 完成：payload 合同全表核定（S_ISSUE/dP_ISSUE=b*2+t 自动；math 8b+2s 恒等编码；MATH_PDS_ACQ=4b+s；LOAD_QDO=4b+g；MAT_ACQ 序号表不动；其余全恒等；span 名 28+provenance=29/29 无增删）；陈旧注释清扫（storage 字节账、pipe_kres/gather 的 score(3)→末 pass 合同、pds 管线 slice 口径、round-gen pair 注释、strip 视图字节恒等式）——其中 gather 冻结区仅注释改动（剥注释 0 行差，freeze-audit 先例）；文件尾 V6 FAT-ATOM ADDENDUM 落笔（论题/几何/协议账/payload 表/判读钩子/预算尺子差异声明 160 vs 224 与 spec 112 vs 176 的计数口径注记）。
[z16] 冻结区回滚修正：批量改名曾波及冻结 offload helper 的 3 处 fold 断言（SUB_TILE_BOX→MATH_SLICE_BOX）——已回滚为原字面 self.SUB_TILE_BOX，并以值锁定遗留别名 SUB_TILE_BOX=16 重新入册（注释标明仅供冻结区，防 booby-trap）；SUB_TILES/SUB_TILE_H 全部剩余引用逐点核验均在手术区内（13512 fragment_C(128,64) ✓）。py_compile OK。
[v6-final] 终报（R-B 合同全案落地，py_compile 全过）：
  总 diff vs fork 基线（v5 终态 bd22106）= +706/−383 行（16,537 → 16,860）。各区：zone1 常量/TMEM/序幕 slice 基建 ~+180；zone1.5 strip 重装箱（host tiler/gmem 视图/W17 环）~+60/−40；zone2 score helper 重写 −137/+185；zone3 math（T2R 分区 −30/+85、主循环 slice 化 ~+90/−60）；zone4 leader ~+40/−30；zone5 注释清扫+尾注 ADDENDUM +88；冻结区回滚修正 ±3。
  协议账（每 bundle 产=耗）：strip 4=4（W17 4 gen ↔ 2 pass × 2 wait/2 release，piece4 界早释保预填重叠）；s_done 2=2、dp_done 2=2（leader 1/pass ↔ math wait@j0/release@j1）；pds 4=4（math 每 slice ↔ 冻结 pair helper 双持双放 ×2 batch）；kres 1=1（release 移 score(1) 后，追踪集恰 S0+S1）；round 20=20、dkv_done 16=16、dq_evict 16=16、dqb_free (N+1)=(N+1)——后四条冻结机器零触碰。发射账（同尺）：score 64 + grads 64 + evict 32 = 160/bundle vs v5 224（score 半减=论题主张；spec 112/176 为块级计数口径，已在 ADDENDUM 注记）。
  冻结区自证：19 个冻结 helper 剥注释逐字节恒等（_offload_dq_block_v52/_issue_dq_block_v52/_drain_dkv_fused_v52/_issue_grads_pair_v51/_issue_dkv_block_pair_v51/_load_chase_piece_v32/_chase_slot_rows_v32/_kd_round_rows_v2/_gather_kdq_v8/_fill_kdq_pair_v8/_fill_kdq_pair_v2/_zero_dq_v2/_store_dq_from_tmem/_store_dq_epi_tma_v12/_get_workspace_size_LSE_OdO/_carve_dq_acc/_drain_dkv_v8/convert_canonical/_split_wg_t1d_v2，脚本化 difflib 审计）；kernel 体内冻结块（gather/relay/G5 wave/TAIL/reduce 角色/W17 round-gen+kdq）hunk 图核验仅 comment-only 触碰（gather kres 边注、relay 零触、G5/TAIL/reduce 零 hunk）；offload helper 曾被批量改名波及 3 处已回滚 + SUB_TILE_BOX=16 值锁别名入册。合同符号：类名字面恒等@11507 ✓；provenance 恰 1 ✓；span 名 28+1=29/29 无增删 ✓。
  自知风险清单（按危险度）：
   R1 strip TMA atom [h32×D256]（(64,32,256) 助手 tiler 经 make_tiled_tma_atom_B——N32 助手 MMA 合法档新值；若 host 拒收症状=trace-prepare 爆，修=按回显调 tiler）与 score-B auto N64 8-stage 绑定字节恒等（host 断言 inner swizzle 相等+cosize；stage 内 n-atom 序 = correctness 门终裁，SCORE-B 先例风险级）。
   R2 N64 fold 假设（col=n%32、DP-half=n//32）——slice≡v5-sub_tile 全案的地基；三个硬件背书形状外推的 N-参数律，若 N64 例外则 correctness 必抓（P/dS 头置换错）。
   R3 slice 源张量 @ 基址+16s 别名 N64 累加器 + 经 evict MMA 的 (128,32) fragment_C 布局——新组合（静态偏移 v32 先例级）；症状=trace-prepare 或 correctness。
   R4 pass 内 s1 等待位于 S_ISSUE 跨内——活性纸面闭合（gen 2t+1 仅依赖 pass t-1 的释放，无回边）；判读时勿把该等待误记为发射税（ADDENDUM 已注记，交叉证人 LOAD_QDO/MAT_ACQ）。
   R5 math 内环 slice_j 层 2 格缩进（规避 300 行整块重排的风格妥协）——零功能风险，后续编辑者注意。
   R6 j==1 的 WAIT_S/WAIT_dP 零宽 span——harness 聚合器容忍度未验（本地账本为权威，v5.2 先例）。
  py_compile：OK（全零区间逐区 + 终态复验）。测试投递权在协调者（~/proxy）。
[v6-r1b] 硬件判决（r1 因 B200 服务中停重投）：correctness 4/4 全过（肥原子机器数学正确、一次过硅）；release 35.591ms vs v5 35.653 = −0.17% ≈ 零改善。**null 结果的强判决**：v5(224条)/v5+red.add/v6(160条) 三个结构迥异候选全落 35.6±0.5ms ⇒ ①"每条 0.2µs 入队税钉死释放版"模型证伪（score 入队砍半不动 e2e——限速真实存在但不在释放版关键路径）；②真 pacer 在三者共享机器中：math/pds 链（v5/v6 逐点相同）、dKV L2 原子流（baseline 同款杀手）、K 驻留翻转、驱逐 drain——四者均未被任何一刀触碰。下一步（零硬件）：v5r10↔v6r1b 双 trace 同税率对账定位不动 pacer。trace 已回传（5.96MB，sha 校验一致）。
[v6.1-diag] S_ISSUE 半程拆发微插桩（协调者，方法论"原子拆分"驱动）：S_ISSUE 同名复用、payload 2→4 粒度（4b+2t+half），D-half 边界的 strip 等待移出 span 变可见 gap ⇒ 单 pass 三段全量化（D-lo 发射 | 边界等待 | D-hi 发射）。r1b 拆分背景：gen3 交叉证人证明 strip 供应 0.6µs 即位（饥饿假说死刑）；payload 算术勘误后 commit 链自洽（S(0) 尾后 +0.61µs、S(1) 尾后 +0.40µs）——S(1) 的 3.97µs 系真实发射/退队限速（~127ns/条 vs 孤立 13.4ns/instr），三轮微基准（dual-acc/SMEM/T2R）全数无罪 ⇒ in-kernel 特有，半程拆发定位堵点在入队口还是退队流。py_compile OK。
[案1中期判决·协调者] 七轮探针全数无罪（dual-acc/SMEM/T2R 远近 acc 靶/FMA/GPU 级 c74 平/全局流 b1 自然实验 0-overlap 同慢）：S(1) 长 = 队列转换（入队钳到退队速率，无罪）× 内核退队 55→100ns/条共驻衰减（涌现性，单原语不可复现）。真实浪费 ~1.5-2.3µs/bundle（周期 3%）非主要矛盾。c148 2× = bench 32 线程块双簇同驻 SM 的假线索（内核 215KB SMEM 不可能同驻）。结案路径改结构性：D128 K 肥化（条数再减半）+ v7 reduce 静默（共驻源消除），并入 v7 战役验收。
