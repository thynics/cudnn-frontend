# V6 build log (fork of v5 @ bd22106; spec V6_FAT_ATOM_SPEC.md)
[atom-price·协调者] B200 实测原子价曲线（mma_atom_price.py，N 扫描，fixed≡rotate）：N32=11.2ns/条(5.61/n16)、N64=13.4(3.35)、N128=17.2(2.15)、N256=34.5(2.16，饱和)。判决：①固定税假说成立，h64=1.68×效率、h128=2.61×、N128 摊平；②累加器复用不串行；③孤立价 11-35ns vs 内核实测入队价 ~200ns = 15×差距，且内核里 G1 小原子与 dVdK 大原子同价 ⇒ 内核 0.2µs 为形状无关的每条前端/仲裁成本（嫌疑：UMMA 前端、TMEM 端口与 math/reduce T2R 争抢）⇒ v6 减条数收益直接兑现；同时标记 v7 级矿脉（200ns→孤立价 = MMA 面 5×+ 余量）。release 预测更新：h64 → ~14µs/64kv ≈ 25ms 档。
[z0] 阅读期完成：spec/V5_BUILD_LOG 全 130 行/HANDOFF §1§3/V32 [13][14]/V5_TILING4 全读；fork 已 cmp 验证 v5==v6 逐字节；教训表 #1-#18 已入脑（#14 三态返回、#16 zip 相位、#17 动态 cosize 禁宿主控制流）。
[z1] 侦察-常量层：SUB_TILES=4@11743, SUB_TILE_BOX=16@11745（语义三重合体：h16 N-half 箱宽=TMEM 槽宽=头解码模数），SCORE_MMA_N=32@11737, SCORE_B_STAGES=16@11775, TMEM 映射@11889-11899（S 32/48, dP 64/80, dkv base 96, BUDGET 352）；score_tiler 由 SCORE_MMA_N 钩子驱动@512, score_b_layout=make_smem_layout_b(...,16)@617。
[z2] 侦察-score 环：helper _issue_score_pass_v5@15088-15214，B 索引 (sub_tile%2)*8+piece@15164/15190，k_blocks=2（mode[2]），(s,dp) 每 pass 单 stage 对=4 commit/bundle ✓ 与 spec 描述吻合。
[z3] 核实中·zone2 关键疑点（未决，正在取证）：strip 几何 = [h16×D512]/stage（STATIONARY_TILE_H=16@11805，2 stage×8192 el/张量）。N64 CG2 的 per-CTA B N-half = h32×D512 = 16,384 el = 两个 strip stage——与 spec 前提"每 h64 子块恰一 strip stage"疑似冲突；且 n32 原子跨 stage 的 n-atom 字节步距 (512,7680,512)el 非均匀，单 MMA 描述符疑似不可表达。正在读 W17 strip gen（14671-14770）与 leader 调用点定罪/翻案。
[z4] 核实-zone3 前提【判定：成立】：P/dS 槽本就 chunk-major [own-kv64×h64]——PDS_BLOCK_ELEMENTS=4096/8192B@11864-11865、storage p_slab/ds_slab 8192 el 双 chunk 子像注释@12177-12188、grads pair helper 按 chunk 像整体消费（4 K16 块 kb∈{0,2,1,3}）@15243-15259。v6 sub_tile(h64)==chunk==v5.1 pair ⇒ grads/驱逐/relay 面零触碰判定成立（若 zone2 活的话）。zone4 轮转（rotation point sub_tile==2@14503 → 改 ==1）与 zone5 payload（15139 处 SUB_TILES 常量自适应）均机械可行。
[z5] 核实-zone1 SUB_TILE_BOX 语义【触发 spec 的"别的语义"分支，记录】：全部 5 处用法均为"头空间 h16 N-half 箱宽"而非"TMEM 列半宽"——(a) head_base j*BOX@13987-13990（头偏移）；(b) 头解码模数 n%BOX + (n//BOX)*SUB_TILE_H@14018-14028×2，其中乘数 SUB_TILE_H(32) 与 rank 步距 32 是 v5 巧合同值，SUB_TILE_H→64 后此解码必错（v6 需乘数→HEAD_CHUNK_GROUP(32)，即 head=t6*64+n 自然映射）；(c) 发布域 (16,2) 列箱@12899-12925；(d) 尾部 t_rs 断言@15724-15756。TMEM 槽宽 16 是独立字面量（TMEM_S1_OFFSET=48 等@11889-11899），不经 SUB_TILE_BOX。此项单独可解（逐点改+乘数换钩），非致命；致命项见 [z6]。
[z6] 【STOP 主罪·zone2 结构性做不通】spec 前提"每 h64 子块恰一 strip stage"对代码为假，肥原子 B 操作数在冻结 SMEM 字节下无合法描述符。证据链：
  (1) strip stage = [h16×D512] = 8,192 el：STATIONARY_TILE_H=16@11805、strip_stage_bytes=16,384B@14684-14688、stationary_q/do 各 16,384 el（2 stage）@12155-12162。
  (2) N64 CG2 的 per-CTA B N-half = h32×D512 = 16,384 el = 恰好两个 strip stage。sub_tile t6 需 h16-tile {4t6+2r, 4t6+2r+1} = strip gen {2t6, 2t6+1} → stage {0,1} 双占（gmem 公式 4*(g//2)+(g%2)+2r@14740-14744）。"恰一 stage"不成立，任何 h64 pass 必然双 stage。
  (3) 描述符代数：n32 的 4 个 (8,64) SW128 核矩阵落位元素偏移 {p*1024, p*1024+512, 8192+p*1024, 8192+p*1024+512}，N 向间距 (512,7680,512) 非均匀 ⇒ 单条 K-major UMMA 描述符（单 SBO）不可表达；auto make_smem_layout_b(N64, 8-stage) 的 stage 字节序（[n32×k64] 连续 2048 el 块）≠ strip 物理序（同 h16 tile 的 k64 块连续）⇒ 直接绑定必错字节。
  (4) 即便走 16 宽窗别名 ((8,2):(1024,0)) 保 spec 索引式 sub_tile*8+piece：每 pass 全程双占两 stage ⇒ W17 无法在 pass0 期间预填 pass1 条带（同字节复用）⇒ 每 bundle 在 score(0)→score(1) 间强插一次全量填充延迟 = 供应对 pass 流水的伪串行化（纪律明令禁止），且直接污染肥原子论题的测量。
  (5) 连带伤：W17 strip 环以 range_constexpr(SUB_TILES) 计数@14706-14708——SUB_TILES 4→2 令 strip gen 掉到 2/bundle、gmem 公式只装 h16-tile {2r,2r+1}，h 高半永不入 SMEM = correctness 必死。spec 手术区未含 W17 strip 区（zone5 反而假设"数目不变"），此依赖 spec 未见。
[z7] 修复选项（供裁决，未动刀）：
  R-A【贴 spec 字面】：手工 16 宽别名 B 窗（n-rest (2,2):(512,8192)）+ W17 环与 SUB_TILES 解耦（保 4 gen 原字节）+ 每 pass 双 strip wait/release。风险：(2,2) 非均匀 N 步距的描述符合法性 = 全新未验风险类（须先投 haifa 宿主编译探针裁决，若 DSL 拒收全案死）；且 (4) 的供应串行化气泡内生不可除——不推荐。
  R-B【贴论题】：strip 重装箱 [h16×D512]→[h32×D256]（同 16,384B 单 box、同 4 gen/bundle：gen g=(t6=g//2, dhalf=g%2)，gmem 窗 = 头 [t6*64+r*32,+32) × D[dhalf*256,+256)，h32-tile 索引 2*(g//2)+rank）+ stationary_tiler (64,16,512)→(64,32,256) + score-B = auto make_smem_layout_b(N64, (128,64,64), 8 stage) 直接绑定——字节恒等式 s*2048 = (s//4)*8192+(s%4)*2048 精确成立，描述符全程 canonical（零新风险类）；B 窗索引 = piece（8 宽末模，spec 的 16 宽式作废）或加手工别名窗保字面式；pass 内早释放 stage0（piece3 后）⇒ W17 预填与 pass 尾重叠，v5 供应重叠形态完整保住；strip 管线 4 产=4 耗（每 pass 2 wait+2 release）；MAT_ACQ 序号表 0-3 不动（zone5"数目不变"满足）。代价：W17 strip gen 块重写（~30 行，不在冻结名单但也不在 spec 手术区）+ B 窗索引合同偏离 spec 文句。推荐 R-B。
  预算复核（两案同）：score 64 条 N64 原子/bundle（2 pass × 8 piece × 2 k_block × G1G2）✓；TMEM 416≤512 ✓；SMEM 字节量全部不变 ✓；math h32 内层消费/寄存器预算不动 ✓。
[v6-STOP] 终裁：按纪律"spec 做不通就 STOP"中止建造。触发点两个：zone1 SUB_TILE_BOX"别的语义"分支（[z5]，可解非致命）+ zone2 结构性矛盾（[z6]，致命：strip h16 几何 × N64 描述符代数 × 供应重叠三者在 spec 字面下不可同时满足）。文件零改动（cmp v5==v6 已验），py_compile OK（fork 基线）。冻结区自证：一个字节未动（全文件未编辑）。建议：裁决 R-B 后复工，届时手术区顺序改为 zone1(常量+SUB_TILE_BOX 逐点) → W17 strip 重装箱（新增区，需授权）→ zone2 score 环 → zone3 math → zone4/5；若坚持 R-A 须先投 haifa 描述符探针。
