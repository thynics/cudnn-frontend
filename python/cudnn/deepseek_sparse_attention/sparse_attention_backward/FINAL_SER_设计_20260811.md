# final_ser 设计文档（2026-08-11，rev2：去借用版）

> 目标：以 `dsa_bwd_sm100_2cta_final.py`（v_w3_2 血统，B200 9.44ms/1.137×）为基座，
> 实现"图二/理想图"流水线的 2-CTA 版本：**同 tile 串行五 GEMM 链 + 高度 overlap**。
> 编号 `final_ser`，文件 `dsa_bwd_sm100_2cta_final_ser.py`，类名保持
> `FlashAttentionDSABackwardSm100TwoCTAV2`（sweep harness `--impl final_ser` 默认类名直接可用）。
>
> rev2（用户 2026-08-11 理想图确认）：**不做 score_kv 复用（dO 借用）**——dO_r0
> 走普通 ring 代，score_kv 每 tile 只装一代 K；借用留作后续优化。

## 0. 定位与诚实预期

final 的旋转调度（S/dP(t) 先发、grads(t-1) 藏其影下）是对"同 tile 串行暴露
softmax 链 ~0.5-0.6µs/tile"（优化设计文档_v2.md:187-208 评审）的规避。final_ser
是该变量的**受控反演**：把发射序改回同 tile 串行，同时把 overlap 工程做满。

台账先例（本设计逐条绕开其死因）：
- v0 纯串行 23.4ms / V1A0 ~10×：死因是 3×48KB 通用 FIFO / 450KB 片上重写，
  不是串行序本身。final_ser 沿用 final 的数据面（驻留/ring/交换/drain/epilogue）。
- v_final_exgh P-early +0.21ms：死因 = 旋转序里 dQ 在梯度块首位等 dS，P 先发
  反而推迟 dS。final_ser 中 dQ 在链尾，dS 有整个 dV 窗口作掩护，依赖方向相反；
  拆 fused 循环的 ILP 税仍在（~0.6µs math），但 math 在串行序里有 dV 窗冗余。
- E1/v_s1 分块流式崩溃：不新增流式通道；KV tile 维持整块 gather（gather 不在
  关键路径，E5b）。
- 预期落点：暴露项 ≈ P 链（S 完→P 发布→DSM 落地→dV）未被 dP 盖住的部分
  + KdQ gather 尾巴（~0.4µs），模型预测 ~5.8-6.3µs/tile ≈ 10.3-11.2ms @2048，
  慢于 final 9.44。价值：(a) 终审"五次单腿手术 standalone≈0"暗示存在 ring 模型
  外的共同定速项，本实验整体更换依赖结构，是信息量最大的单次实验;
  (b) GR100 上 82% stall 是同步自旋，串行链大幅简化同步结构。

## 1. 理想图 → 代码的逐块映射

| 理想图元素 | final_ser 实现 |
|---|---|
| Load Q / Load dO（序幕） | 不变：W17 stationary TMA，Q/dO 分离就绪（S 只等 Q，dP 只等 dO） |
| Gather KV（tile 头） | `_load_score_kv` 整块 gather K(t) 进 score_kv（K=V，一次 gather 喂 S 和 dP；不分块流式）。**无借用：score_kv 每 tile 一代 K**，K(t+1) 在 dP(t) 释放后立即 gather，被 dV/dK/dQ 全窗盖住 |
| S=QK → dP=VdO | 不变：`_issue_score_v2` ×2，CG2，TMEM ping-pong |
| dO0..dO3 / Q0..Q3 | round ring 8 代 TMA fill（own 半 bulk s2cluster + peer 半 TMA，L2 热），**顺序 = 消费序**：dO_r0 对 → dO_r1 对 → Q_r0 对 → Q_r1 对 |
| K0-K3 再载（dQ 前） | = KdQ 融合双轮 gather（`_gather_kdq_v8`），**ring 尾部 g8/g9**（共 10 代/tile，10%2=0 相位铁律保持），dK 窗口作掩护，dQ(t) 同 tile 消费 |
| P → exhg | math **P 相**：T2R S → exp2 → stmatrix P blocks/xchg → fence → `p_ready_mbar`(新, count-128) → W18 立即 DSM 发 P → 落地 → relay0（语义与 final 一致，只是提前到 dP 影子下） |
| dS → exhg | math **dS 相**：T2R dP → dS → stmatrix ds blocks + ds_image → fence → `pds_ready_mbars`(沿用) → W18 发 dS → pds commit → 落地 → relay1 |
| dV | dV r0（ring dO_r0 × p_blocks，门：relay0）→ dV r1（ring dO_r1 × p_blocks），slot0/slot1 各 ACC=False 初始化 |
| dK | dK r0（ring Q_r0 × ds_blocks，门：relay1）→ slot0 commit → dK r1（ring Q_r1 × ds_blocks）→ slot1 commit（dV+dK 融合同一 TMEM 槽，K=V） |
| dQ | `_issue_dq_rounds_v2` 双轮（ring KdQ0/1 × 本地 ds_image），TMEM 跨 tile 累积；dq_done commit 在循环后（串行序里 dQ 本就是末 GEMM，时机与 v12 early-commit 等价） |
| reduce | 不变：`_drain_dkv_v8`，slot0 在 dK r1+dQ 窗 drain，slot1 滑进下一 tile 的 S/dP 窗 |

## 2. 每 tile 稳态时序（leader 视角，无旋转）

```text
kscore.wait(K(t)) → S(t) → dP(t) → kscore.release
      [gather: 观察释放 → gather K(t+1) → commit（dV/dK/dQ 全窗作掩护）]
      [math:   S done → P 相 → p_ready；dP done → dS 相 → ds_ready]
      [W18:    p_ready → 发 P → P 落地 → relay0；ds_ready → 发 dS → pds commit → dS 落地 → relay1]
relay0.wait → dkv_done.acq(slot0) → ring×2(dO_r0) → dV r0 ×2
dkv_done.acq(slot1) → ring×2(dO_r1) → dV r1 ×2
relay1.wait → ring×2(Q_r0) → dK r0 ×2 → dkv_done.commit(slot0)
ring×2(Q_r1) → dK r1 ×2 → dkv_done.commit(slot1)
      [gather+W17: ring g8/g9 信用 → kdq_barrier 会合 → KdQ(t) 融合双轮填 → kdq_ready]
ring×2(KdQ) → dQ r0/r1（ACC=跨tile）→ pds.release；[循环后: dq_done.commit + tails]
```

关键掩护账（µs，B200 口径）：
- dO_r0 环填（~0.3×2）在 dQ(t-1) 释放 g8/g9 后立即开始 ⊂ S(t)+P math 窗 ✓
- dS math+发（~1.6-2.0）⊂ dV r0+r1 执行窗 + dK r0 等待自然后移，微暴露
- K(t+1) gather（~1.3）⊂ dP(t) 释放后的 dV+dK+dQ 全窗（~3.5）✓（比借用方案更早）
- KdQ(t) gather（~0.4-0.5）尾部暴露 ≈ 0.4/tile —— 已知税，见 §0
- ring 供给：10 代/tile × ~0.3µs 填时，W17 恒领先 2 代，总供给 ~3µs < 周期 ✓
- reduce：slot0 commit 后有 dK r1+dQ+下一 S/dP ≈ 3µs 窗 > drain 2.5 ✓

## 3. 同步结构 diff（相对 final，最小手术原则）

新增：
- `p_ready_mbars` Int64[1]（count=MATH_THREADS=128，+8B SMEM，slack 1536B 内）

语义改变（结构不变）：
- `pds_ready_mbars`：从"P+dS 全部发布"变为"dS 相发布"（ds_ready）
- W18 relay 拆两腿：P 腿（等 p_ready → 发 P → 等自身落地 → arrive relay0）、
  dS 腿（等 pds_ready → 发 dS → pds commit → 等落地 → arrive relay1）；
  relay_mbars/landing_mbars/pds commit 位置全部照旧
- pipe_round 代序：`dO0,dO1,dO2,dO3,Q0,Q1,Q2,Q3,KdQ0,KdQ1`（10 代 % 2 stages == 0）；
  W17 g0..g7 TMA、g8/g9 会合提交；W19 转发 g0..g7、跳 g8/g9
- pipe_kscore：每 tile 一代 K（原 K+loan 两代）；gather 每 tile 程序序 =
  K(t+1)（若非末 tile）→ KdQ(t) 会合；tile_count==1 特判与 noop-recommit 删除
- leader：删除旋转（prev_grads head/tail + 循环外末 tile drain）；
  新 `_issue_grads_ser_v1`（全 ring 消费）；dkv_done 拆 acquire/commit 双
  producer state（镜像 reduce 侧 split wait/release 先例）；dq_acc = loop_iter != 0；
  dq_done commit 移到循环后
- math：fused 单循环拆 P 相 / dS 相两段（grouped-stats 索引两相共用）

完全不变：host `__call__`、SharedStorageV2 布局（仅 +1 mbar 字段；loan 字段保留
未用，供后续复用优化）、TMEM 图、DKV/DQ MMA tiler 与 pass 结构、drain、
dQ TMA epilogue（loan_epi_safe 协议照抄，现锚定末代 K 的 producer_tail）、
convert_canonical、sum_OdO/sum_dSink、reverse tile 序、零 tile 退化路径、
kdq count-128 硬件到达协议（R2）、OWN_HALF_BULK、stmatrix 发布布局与字节恒等断言。

## 4. 已知风险与对应门

| 风险 | 门/缓解 |
|---|---|
| math 拆相 ILP 税 ~0.6µs（exgh 实测） | 串行序下 math 冗余度 = dV 窗口；若 trace 显示 ds_ready 迟于 dK r0 需求点，回退：dS 相先算后存 |
| KdQ 尾部暴露 | 已计入预期；后续旋钮：KdQ(t+1) 提前一拍（9 代方案）或恢复 score_kv 复用装 KdQ |
| dO_r0 进 ring 使供给 +2 代 | 填时 ~0.6µs 增量,在 S+P 窗内;若 W17 供给成为 pacer,后续恢复借用（rubin_1 逆操作） |
| slot0 commit 后移拉长 reduce 首启 | slot0 drain 窗仍 ≈ 3µs > 2.5 ✓ |
| 相位记账（kdq_phase/relay_phase/landing_phase 随代序变化） | 对抗审查 workflow 逐相位复核 + trace 版本插桩位映射 |
| PTXAS 寄存器（leader 串行块） | 叶子复用不展开新循环；沿用 unroll=4 教训 |

## 5. 验证协议

1. commit + push 后 `./benchmark/dsa/run_b200_pipeline.sh --impl final_ser`
   （四门正确性 + release 性能 + 双 IKET + span 表；成功判据 exit 0 + DSA_PIPELINE_PASSED）。
2. 判读：与 final 同机同轮对称口径；关注 (a) period 相对 5.33µs/tile 的增量分解
   （P 链暴露 vs KdQ 尾巴 vs 意外项），(b) 后续 GR100 对比 sync-spin stall 占比是否
   从 82% 下降——本实验的科学读数。

## 6. 实测判决与事后分析（2026-08-11，B300 SXM6 AC / cc 10.3）

- 正确性：四门全过；对 baseline 交叉核对 **dQ 位级零差**、dKV 差一个累加序 ulp（0.000488）。
- 性能（fair_direct_program_only，24 对 ABBA）：baseline 7.630ms，**final_ser 11.114ms
  （494.7 TFLOPS），1.4565×（−45.65%）**。
- 对抗审查复核（9 agent）：记账/死锁/数值/DSL 四维度零 HIGH；
  确认两项模型缺陷——(1) §1/§2 的"P 交换藏进 dP 影子"声明定量为假：dP 影子仅
  ~0.35µs，P 发布链（T2R S 0.6-0.8 + 拆相 exp2/downcast 1.0-1.4 + stmatrix 0.3-0.4
  + count-128 尾 ≤0.448 + W18 腿 0.3-0.45）≈ 2.6-3.6µs，**relay0 前的 TC 气泡
  ≈ 2.2-3.2µs/tile，是串行形态的主导结构项**；(2) §0 预测带 5.8-6.3µs/tile 由此
  不可达。静态复核模型（中位 8.6µs/tile）反向偏悲观；实测 ≈ 6.3-6.8µs/tile
  介于两者之间——最终逐项定价需 span 实测（见 trace 协议）。
- B300 放大效应：baseline 从 B200 8.62 → 7.63（算力缩短 GEMM），而暴露项是
  延迟型（math 链/DSM/集合 arrive/gather），不随算力缩放 → 串行税的相对占比
  随硬件代际上升（Rubin 上会更差）。旋转的必要性在新硬件上更强，不是更弱。

## 7. IKET A/B span 分解（2026-08-11，B300，稳态 tile 10-24）

周期：baseline 4.45-4.50µs/tile vs final_ser ~8.0µs/tile（IKET-on 口径，方向与 release +45.65% 一致）。
结构验证：单 tile 五 GEMM 串行链与设计一致；K(t+1) 预取提前 ✓、dS math 藏进 dV 窗 ✓、
reduce 原子体反而更快（0.934 vs 1.503，rank 劈半兑现）✓、S/dP 发射更快（0.44/0.50 vs 0.55/0.59，CG2 兑现）✓。

暴露项实测（µs，均值）：
| # | 项 | 实测 | 旧模型 | 修正 |
|---|---|---|---|---|
| 1 | dP 末 → 首 dV（relay0 门） | **1.77**（其中 P relay wait 1.43） | 2.2-3.2 | P math 本身仅 0.34（≈baseline 0.31）——**链的大头是发布+集合+DSM+落地（P publish 0.50 vs baseline 0.066），不是 math** |
| 2 | ring 趟间隙（dV 内 0.98 + dK 内 0.73） | **1.70** | ~0（假设 W17 恒领先） | **新发现**：10 代/深 2 供给率跟不上背靠背消费 |
| 3 | 末 dK → 首 dQ（KdQ 尾） | **1.27**（RING_WAIT 包络 1.72） | 0.4 | 低估 3 倍：会合链（信用→barrier→gather→count-128→commit）整体计入 |
| 4 | 末 dV → 首 dK（relay1 门） | 0.84（dS relay wait 0.87） | 微暴露 | dS math 0.99 vs baseline 0.18（标量 vs packed 的真实差在 dS 相），大半被 dV 盖住 |

杠杆重排序（按实测收益）：
1. **借用回收（loan 复用装 dO_r0，ring 10→8 代）**：同时打 #2 和 #3（KdQ 会合提前一拍），预计 −1.2~−1.8；
2. **count-64 子门 + 发布链重构**：打 #1 的 0.5-0.9（DSM+落地 ~0.4-0.6 是结构地板）；
3. **E2 packed（已建 final_ser_e2）**：打 #4 经 relay1 传导，预计 −0.3~−0.5（比初估低——P math 已证不是主项）。
全部兑现估计 ~8.0 → ~5.5-6.0µs/tile，仍高于 baseline 4.5（结构地板 = 交换链 + KdQ 物化 + 无免费转置）。

## 8. B300 复审对账与修复矩阵(2026-08-11 晚)

复审(B300 侧)七项 vs 本侧行动:
- P0 spill(272B 栈帧、20M 动态 local load):**final 血统继承的 pre-K1a 寄存器分配(48/48/128/128)**;
  修复 = K1a (64/64/128/120)(GR100 局部最优,4f683ca/k13)。注意 spill 热点含 W17/roundbuf/reducer——
  ring 趟间隙与 reducer STL 可能部分是 spill 税,K1a 单变量读数须看这些 span 的联动。
- P0 Knext/KdQ 顺序(gather 先 wait_group(0) 等完 K(t+1) 才填 KdQ):kq 的 gather 序 = KdQ-first,结构性覆盖。
- P0 roundbuf 10 代/2 槽、P0 串行主链 22% 基本盘:共识;前者 kq 修(8 代 + KdQ 走 kscore),后者为受控变量。
- P1 W18 过度串行:复审的"dS 提前发"有 relay0 被 pds_ready 拖住的隐患;改用**双 lane**(lane0 P 腿 ∥ lane1 dS 腿,
  pds commit 随 dS 腿),relay0 零风险,dS 发送不再排队。
- P1 reducer credit(slot0 burst 完才碰 slot1 + 2M STL):**缓**——重排翻倍 drain 寄存器压力,恰逢 spill 现行;
  K1a 后重测再决。
- P2 标量 softmax:e2 实测 1.03%,归档(与 span"P math 两边几乎相等"互证)。

变体矩阵(类名均为 FlashAttentionDSABackwardSm100TwoCTAV2):
| 变体 | 内容 | 单变量目标 |
|---|---|---|
| final_ser | 基准(8.0µs/tile) | — |
| final_ser_k1 | own-rank arrive → 本地编码(4 处) | P/dS 关门 + relay 链 |
| final_ser_k1a | K1a 寄存器 (64/64/128/120) | spill(看 SASS STL/LDL + ring 间隙/reducer span 联动) |
| final_ser_e2 | packed f32x2 math | 已裁决:1.03% |
| final_ser_kq | k1 + K1a + 调序(dV→dQ→dK)+ KdQ 借宿 score_kv + ds_local_ready 门 + ring 8 代 + 双 lane W18 | 合成候选 |
挂起:reducer credit 重排、count-64 半门、PipelineTmaUmma tx-commit(kq step2)。

### §8 附:第一轮变体判决(B300, alloc 3592663)
- **final_ser_k1 = 零收益**(11.177ms,比 ser 腿 ±0.21% 噪声;正确性四门+crosscheck 全过)。
  **cluster 编码假说被硅上证伪**——P_PUBLISH 0.48µs 的膨胀不是 arrive 编码,残余嫌疑人:
  (a) spill(math 也在 272B 栈帧热点里),(b) 线程参差(无前置 barrier 对齐),
  (c) count-128 尾本身(e12 的靶)。**k1a 的优先级因此上升**(唯一能同时解释多科目的候选)。
- **final_ser_kq 编译失败已修**:kdq_loan 裸 recast_ptr 丢失对齐推断(2B),cp.async 128-bit
  gather atom 拒绝;改用 make_ptr assumed_align=1024 惯用法(aligned_p_blocks 先例;
  score_kv 基址 1024 对齐,+16KiB 半区偏移保持)。

### §8 附 2:k1a 首轮判决与修正(B300)
- SASS despill **成功**:栈帧 272→120B、静态 LDL −71.5%、STL −66.7%——spill 假说的编译层证据成立。
- 运行时 **setmaxregister 死锁**:我的预算算错——setmaxregister 只能在 **CTA launch 分配
  (640×96 = 61,440)** 内重分配,不是 65,536 物理文件(仓内约束 v9_3.py:12440);
  (64/64/128/120)=63,488 超 2,048 → increase 永久等待。
- 修正:**48/64/128/120 = 恰好 61,440**(gather 保持 48;= rubin_1.py:102 的正牌 K1a)。
  k1a 与 kq 已同步修正。教训:B300 上 setmaxregister 超订不是回退到 spill,是**挂死**。

### §8 附 3:第二轮判决(B300)
- **final_ser_k1a = 10.751ms(−3.18% vs ser)**:despill 兑现为真实 perf;寄存器假说成立但非主导。
- **final_ser_kq = 9.709ms(−12.57% vs ser,1.263× vs baseline 7.687)**:调序+KdQ 借宿+8 代 ring
  +双 lane W18 的合成收益 ≈ −1.04ms(叠在 k1a 之上);周期估算 8.0 → ~7.0µs/tile。
- 归因链定格:k1(编码)≈0、e2(标量数学)1.03%、k1a(spill)3.18%、kq(调度/供给)~9.4% 增量。

### §8 附 4:kq span 判决(B300 IKET,稳态 tile 10-24 中位)
| span | ser | k1a | kq | 判决 |
|---|---:|---:|---:|---|
| WAIT_dQ | 0.992 | 1.024 | **0.032** | KdQ 尾巴清零(借宿+ds_local 门兑现) |
| WAIT_dK(reduce 侧) | 3.232 | 3.200 | **1.536** | 减半(供给提前) |
| WAIT_RELAY | 1.216 | 1.312 | **0.928** | 双 lane + 调度收 0.3 |
| ring 趟等待 | 0.98/0.73(ser 推算) | ≈同 | **WAIT_ROUND 0.064/EMPTY 0.112** | 趟间隙基本清零(8 代 + despill) |
| WAIT_KSCORE(dQ 等 KdQ) | — | — | 0.128 | KdQ 预填充分就位 |
| WAIT_dQ_GATE(ds_local) | — | — | 0.384 | 新门的可见成本(可再压) |
| k1a 各 span | ≈ser | ≈ser | — | despill 收益均摊,无单点 |
剩余暴露 ≈ relay0 0.93 + dQ 门 0.38 + 杂项;kq 周期 ~7.0µs/tile vs baseline 4.47。
正确性缺口未补(本轮均 perf-only)——升级任何结论前必须先过四门。

### §8 附 5:relay0 攻坚(kq2/kq3,2026-08-11 深夜)
- relay0/relay1 拆分实测:**relay0(dV 门)= 1.18-1.23µs = 全部;relay1 = 0.06-0.11(dQ 窗掩护兑现)**。
- **kq2** = kq + count-4 关门(e12 裁决:fence → sync_warp → elect arrive,mbar 计数 128→4)。
- **kq3** = kq2 + 一步到位三件套:Rep(8) T2R(baseline 原子)+ **image 直发**(P 得到 p_image,
  预算 = 删 ds_xchg + 退役 p_xchg,净 0;owner-split/grouped-banding 机器整套退役,
  math 发布无分支)+ relay lane 双拷贝装配(own 半本地 bulk + peer 半 DSM,landing
  count 1→2 / tx 2×4096)。W18 折叠评估后放弃(唤醒已被双拷贝摊销,折叠会以
  close 参差拖累 dS 相)。
- kq 孪生补齐 math 探针 9 名(WAIT_S/T2R_S/P_MATH/MATH_PDS_ACQ/P_PUBLISH/WAIT_dP/
  T2R_dP/DS_MATH/DS_PUBLISH),词表 19→28 ≤ 29 上限;P 链内部自此直读。

### §8 附 6:kq2 判决 + kq3 rev2(B300 job 3592663)
- **kq2 = 9.783ms,1.2726×,四门+crosscheck 全过;与 kq(1.2730×)持平 → e12/count-4 证伪**。
  关门三连尸检:k1(编码)≈0、kq2(计数)≈0 ⇒ P_PUBLISH 0.48 的本体是 **warp 间完成参差
  +(疑似)T2R 吞吐**,不是 arrive 机制。
- **final 的 B300 锚点(本轮新数据):≈9.14ms(1.188×)**;kq2 距 final 仅 7.06%(≈0.4µs/tile)。
- kq3 Rep(8) 编译失败:V2 的 CG2 score 累积器是打包 TMEM 片段 (32,2,16),仅 Rep(4)
  的 (64,16) 瓦片可整除;baseline 的 Rep(8) 靠的是 CG1 M64 的不同几何。真上 Rep(8)
  需 64 列非打包布局 = 丢 S/dP ping-pong(串行序下本无用;64+64+256+128=512 恰满)
  ——立项为 kq4,原子空间调查进行中。
- **kq3 rev2**:回退 Rep(4)、恢复 grouped-stats;保留 image 直发 + relay 双拷贝装配
  (landing count-2 / 2×4096 tx)。可编译,待验。

### §8 附 7:kq3 rev2 判决 —— image 直发在 Rep(4) 下为负项
- 9.974ms(1.2975×),比 kq2 慢 1.95%;正确性全过。
- 机制:旧 owner-split 让 own 半在 math 发布期间"顺手"写进 blocks(与 stores 并行,免费);
  kq3 把它挪到 relay lane 的 close 之后串行执行(本地 bulk 计入 landing 的 tx)——
  在 relay0 门上净增一跳 bulk 延迟(~0.13µs/tile)。发布无分支的节省(warp-uniform 分支本就便宜)不足以抵消。
- 处置:**kq3 挂起留树**,作为 kq4(非打包 TMEM + Rep8)的配套件——Rep8 的 ownership
  非 n-half-uniform,届时无分支发布是必需而非可选;standalone 判负。
- **当前最佳:kq2 = 9.783ms(1.2726×),距 final(9.14)7.06%。**

### §8 附 8:kq3 rev2 深度尸检(目标-交付对账)
**原目标**:relay0 = 1.18-1.23µs(dV 门,P 链净暴露)。作战计划三件套:
Rep(8) T2R(主武器,打 T2R+math 0.70)/ count-4 关门 / 发布重构(Rep8 的使能件)。

**交付对账**:
| 计划件 | 是否发射 | 结果 |
|---|---|---|
| Rep(8)(主武器) | **未发射**(TMEM 打包几何编译墙) | 目标段 T2R_S+P_MATH 0.70 **原封未动** |
| count-4(kq2 底座) | 发射 | ≈0,已证伪 |
| 发布重构(使能件) | 发射了**没有载荷的使能件** | 使能件自身成本 +0.117µs/tile 直接落在 relay0 链上 |

**建模错误(可预防)**:owner-split 的 own 半写入发生在 math 发布窗内(并行 lane,零链上成本);
我把它挪到 relay lane 的 close 之后(门控链内,且 tx 计入 landing)。我自己的净暴露记账法
本可在实现前抓住这一点——"下游契约不变"的判断没错,但**链长变了**。教训:任何操作数
装配的搬移,实现前必须先分类"并行 lane vs 门控链"。

**原问题现状:未解决且未被触碰**。relay0 的链分解(k1a span + 传输审查):
T2R_S 0.352 + P_MATH 0.352(主武器未发射)| warp 参差(**从未直接测量**)|
close(两刀证伪,≈0.1 残余)| W18+DSM+落地 ~0.4-0.5(结构)。

**对 kq4 的预算修正**:净收益 = Rep8 T2R(−0.4 期望)+ 参差改善(未知)
− 装配税(−0.117 已实测,除非找到更优装配)。调查组待办追加:各候选原子下的
per-warp ownership 足迹,以及是否存在保留"math 窗内 own 半写入"的装配形态。

**参差测量方案**(下次 kq 系捕获免费获得):补过 math 探针的孪生里,
P_PUBLISH(i) 的 per-warp 结束时刻已可提取——四个 math warp 的最晚-最早差即参差,
不需要新探针。

### §8 附 9:kq4 四变体(单变量,基于 kq2)
| 变体 | 单变量 | 靶 | 预期 |
|---|---|---|---:|
| kq4a | 融合 T2R(S+dP 背靠背、单 fence、release 随后) | T2R 暴露往返(latency-bound 判定) | −0.15~0.25 |
| kq4b | release 出链(s/dp_done 释放移到各自 math 后) | span 内 release 税 | −0.05~0.1 |
| kq4c | packed math(e2 循环,现落在 relay0 链上) | P_MATH 0.352 | −0.10~0.15 |
| kq4d | xchg 早发(p_xchg 仅 2 个非 owner warp 写 → count-2 窄关门即发,relay0 前补等全量) | 关门尾+DSM 飞行重叠 | −0.10~0.2 |
背景判决:Rep8/换原子几何死刑(CG2 打包 (64,(32,2)) 的"2"在 lane 维);unpack 无旋钮;
自旋无退避可调;传输整发形态为地板。kq4d 强形式(每 warp 2KB 增量发)待布局连续性
探针(agent 进行中)。结构结论:掩护(S 前移)与 KdQ 借宿在 227KB 下互斥——
形态地板 ≈ relay0 0.6-0.9,对应 wall ~9.2-9.5(final=9.14)。

### §8 附 10:kq4a 判决 —— 融合 T2R 击毙(+3.7%)
- 10.143ms(1.3197×),正确性 PASS。
- 尸检:融合把"等 dP 完成"搬进了 P 链前段。旧序里 math 到达 dP 等待点时(P 发布之后,
  ~S+1.5µs)dP(S+0.35 完成)早已就绪,WAIT_dP=0.224 纯唤醒;新序里 math 在 ~S+0.15
  就到达该等待点,dP 还在张量核上 → 裸等 ~0.2µs,恰好加在 relay0 链上,吃掉重叠收益还有余。
- 方法论教训(与 rev2 同构,第二次):**位置依赖的测量不能外推**——"WAIT_dP 是纯唤醒"
  只在旧位置成立;搬动等待点前必须重算它的到达/就绪时刻差。
- 反向收获:**分相(P-early)结构在 T2R 排序上已是局部最优**——S 链要快就只能缩段本身,
  不能重排。kq4a 不进合并。

### §8 附 11:kq4d 强形式探针判决 —— 非法,弱形式为最终形态
- GPU-free 布局探针(probe_kq2_pxchg_warp_footprint.py,双路地址核算 0 失配):
  **每个写 xchg 的 warp 的足迹不连续**——swizzle 下 28-29 段 64-128B 小段,与兄弟 warp
  以 64B 粒度交错;唯一连续单元 = warp 对的整 4KB(现行设计)。半 warp 粒度更碎。
- per-warp 2KB 增量发送需要 28+ 条 bulk 小操作,或重设计外层布局把 h 半块化成 2KB 板——
  但 store 布局与 dkv_b 操作数布局的字节恒等是 MMA 侧锁死的 → 强形式**结案:非法**。
- kq4d 保持弱形式(count-2 窄关门早发,已建成待验)。

### §8 附 12:kq4b 判决 —— 小赢
- 9.7509ms,PASS,比 kq2 快 0.33%(−0.03ms ≈ −0.02µs/tile)。release 出链方向正确、
  量级如预期偏小(预期 −0.05~0.1µs/tile 的下沿以下);进合并候选。

### §8 附 13:kq4c 判决 —— 中赢,当前最佳
- 9.6286ms,PASS,比 kq2 快 1.58%(−0.155ms ≈ −0.10µs/tile),落在预期带(−0.10~0.15)正中。
- 印证:e2 的 packed 刀本身没错,当年 1.03% 是链位问题;同一把刀落在 relay0 链上立刻兑现。
  P_MATH 0.352 的可压部分已收割;进合并。

### §8 附 14:kq4 四腿终判(B300 job 3592663,四腿正确性全过)
| 腿 | ms | vs kq2 | 判决 |
|---|---:|---:|---|
| kq4c packed math | 9.6286 | **−1.58%** | ✅ 唯一明确保留 |
| kq4b release 出链 | 9.7509 | −0.33% | 小赢,待复跑确认 |
| kq4d xchg 早发 | 9.8924 | +1.12% | ❌ 击毙 |
| kq4a 融合 T2R | 10.1430 | +3.68% | ❌ 击毙 |
- kq4d 尸检:窄关门早发的前提是"非 owner 先关完、飞行盖 owner 尾巴";实测输 →
  **四个 math warp 关门几乎同时(参差小)**,早发无利可图,而新增机械
  (sync_warp+额外 arrive、W18 双 mbar 等待、发送与 owner stores 的 SMEM 争用)是纯成本。
  **附带情报:P_PUBLISH 的 ~0.45 残余是真实工作(store/fence 本体),不是等人**——
  参差假说出局,发布段结案。
- 结构结论更新:relay0 链的全部科目已逐段裁决(重排×、机制×、参差×、math ✓ 已收、
  release ✓ 已收、传输=地板)。**kq4c ≈ 串行形态的实际地板**;距 final(9.138)的
  5.37% = "尾巴换掩护"结构交易的价格。
