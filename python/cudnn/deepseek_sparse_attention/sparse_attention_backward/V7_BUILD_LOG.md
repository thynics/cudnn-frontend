# V7 build log (fork of v6 @ a3dd79c; spec V7_RESIDENT_DQ_SPEC.md)
[z0] 阅读期完成（spec 56 行全读 / V6 log 49 行全读 / V5 log 130 行全读；fork cmp 逐字节==v6 a3dd79c 已验）。教训入脑：#14 jit 状态返回重赋值、#16 zip 相位、#17 动态 cosize 禁宿主控制流、fix-r7 静态槽址（禁 align/运行时槽算术）、#18 wait 型 span 禁入供需算术。复活参照物已从 git 21118ba（v5.1b 终态=驻留 dQ 原形）提取至 scratchpad：pipe_dq_done 创建(13110)/t_dq 双张量(13217)/_issue_dq_wave_v32(15063)/leader G5 双 round 环(14308-14366)/TAIL dq_done commit(14369-14384)/math 尾部 DQ_EPI(r) 双段(13962-14014)/dq fragments legacy 形(12970-12976)。关键侦察结论：
  (a) dq_evict_tiled_mma 不能整体退役——冻结 math 消费环的 slice 机器（rank_slice_mma@13158、score_slice_c_layout@13527、(128,32) fragment_C fold）以它为宿主。裁定：DQ_EVICT_TILER=(128,32,64) 常量以"值锁定"保留（SUB_TILE_BOX 先例），注释改口径为 math-slice fold MMA；DQ_EVICT_ROUNDS/WINDOWS/SLOTS 三常量退役删除。基座 __call__ 的 guard/断言/内核签名不动（V0/V1A0 共享签名）。
  (b) DQ_EPI span 名逐字核验 == "DQ_EPI(r)"（offload 版与 v5.1b epilogue 版同名）——29/29 名集自动保持。
  (c) v7 TMEM map 定案：S[0,64)(S1=32) | dP[64,128)(DP1=96) | dkv 2×64 [128,256) | dQ0=256/dQ1=384 [256,512)；BUDGET=2*32+2*32+2*64+256=512 整；__call__ 断言改 == TMEM_COLUMNS 带双值回显。
  (d) s_dq_epi 休眠视图（round_buf_a+b 连体 32KB@12784）与 v5.1b 逐字同构，复活仅改注释；dq_b_t@12853 现为休眠绑定，复活为 dq_ds_fragment 源。
  (e) dkv 环 4→2 = v5.1b 原生 MMA_DONE_STAGES=2 形态回归 + v5.2 融合 drain 保持（双 wait/双 release 与环 2 相容：16 块/bundle mod 2=0 相位律）；_issue_grads_pair_v51 v7 解冻（spec 手术区 3），签名 4 槽→2 槽。
  (f) workspace：V2 _carve_dq_acc/_get_workspace_size_LSE_OdO 双覆写删除→基类 None 路径 + 8B entry（接口分配点 max(impl,2312) 钳制不变，分配语义无扰）。
  手术序：Z1 常量/TMEM/__call__ 断言 → Z2 存储/序幕（管线+fragments+t_dq/t_dkv_army）→ Z3 dkv 环 4→2（helper 签名+双调用点+drain 调用点）→ Z4 G5 驻留+_issue_dq_wave_v32 复活+_issue_dq_block_v52/_offload_dq_block_v52 删除+leader TAIL+math 尾部 epilogue+reduce 纯 drain 化 → Z5 注释清扫+payload 表+尾注 V7 ADDENDUM。逐区 py_compile。
[z1] Z1 常量层完成：G5 平面注释改驻留口径（v12 血统/4 wave MMA/单次终舍入账目）@11869 区；DQ_EVICT_TILER 值锁定保留（math-slice fold 宿主注记）、DQ_EVICT_ROUNDS/WINDOWS/SLOTS 三常量删除；TMEM map 重排 S[0,64) dP[64,128) dkv[128,256) dQ0=256/dQ1=384，BUDGET=512 整（账目注释重写含环 2 节流窗与 STOP 门 5µs/对）；MMA_DONE_STAGES 4→2（p%2 槽律注释）；__call__ 断言改 == TMEM_COLUMNS 双值回显；V2 双 workspace 覆写删除（基类 None 路径 + 接口钳制注记，lesson #17 指针留 V5_BUILD_LOG）；存储 dkv_done_mbars 8→4、dq_evict_mbars 删除、dq_done_mbars 复活注释。py_compile OK。残留（Z2 处理）：dq_evict_mbars@13391（管线创建）、TMEM_DQ_SLOT0/1@13565/13569（t_dq_rot）。
[z2] Z2 序幕完成：s_dq_epi 注释复活（v5.1b 逐字口径：round_buf_a+b 连体 32KB、dq_done commit 即死区）；round_kd_evict 双视图删除（G5-A 回冻结 round_kd legacy 视图）；G5 fragments 回 v12 形（dq_kd_fragment_a/b = dq_tiled_mma.make_fragment_A(round_kd[0/1])、dq_ds_fragment = make_fragment_B(dq_b_t)——dq_b 双像（dq_b_evict_layout/评断言/dq_b_evict_t）整体删除）；pipe_dq_evict → pipe_dq_done（1 stage，leader→math_group，dq_done_mbars，v12 群提交注释）；dq_evict_c_layout 删除；t_dq_rot → t_dq 持久双张量（dq_c_layout @ 256/384）；t_dkv_army 4→2 元组（p%2 槽律注释）；内核头 _ = dq_a_evict_layout_staged / _ = mdQ_acc（共享基座签名不动、dq_evict_tiled_mma 存活为 slice fold 宿主注记）。py_compile OK。dq_b_raw 存留核验：12963/12985/15097 全为冻结 relay/发布机器。残留（Z3/Z4 处理）：roles 区 evict 状态/调用/G5 块 + 尾注。
[z3] Z3 dkv 环 4→2 完成：_issue_grads_pair_v51 签名 4 槽参数→2（t_dkv_s0/s1）、槽选择块 (2r+p)%4 元组索引→p%2 直指（t_slot_dv=s0/t_slot_dk=s1，slot_tensors 元组删除）、docstring 依赖边改"TWO generations back/环 2 节流窗注记"；leader 双调用点同步（army[0]/[1]）；reduce 融合 drain 调用点 army[(2r)%4]/[(2r+1)%4] → army[0]/[1] + 注释；冻结 drain 本体唯一触碰 = docstring 一句（4-deep→2-deep，comment-only，_load_chase_piece_v32 先例）——代码剥注释恒等待终验自证。协议重审：leader acquire gen n ← drain release gen n-2（T2R+fence 后即放、原子流在释放外），16 产=16 耗 mod 2=0 相位闭合，初始信用 2 由 MMA_DONE_STAGES=2 管线初始化自动供给。py_compile OK。
[z4] Z4 G5 驻留手术完成（本次战役主刀）：
  - reduce 角色纯 drain 化：offload 环（evict_wait/evict_rel 状态 + 16 块三重循环 + is_first/is_last）整体删除，角色头注改"纯原子流=baseline 同价 15.6µs/kv128"口径；
  - leader：dq_evict_prod → dq_done_prod（1 stage）+ 环前单次 producer_acquire（v5.1b 形，credit 合同注记）；G5 块重写为 v5.1b 驻留形——r0 环（round wait → dqb 门 per-wave @ dqb_parity → ACC 选择（wave0: loop_iter!=0 / wave1: True）→ _issue_dq_wave_v32(t_dq[0]) → round release）+ r1 环（同形无门 t_dq[1]）+ dqb_free 群提交（errata #1 形注释保留）；kdq gen 序号注释修正为 g16+w/g18+w（v5.1b 原文残留 g32 陈旧序号未带入）；
  - TAIL 回 v12 形：dq_done producer_commit+advance 前置，producer_tail 表 {s,dp,dkv,dq_done,dqb_free}（dq_evict tail 删除）；
  - math：dq_done_state 新增；尾部 epilogue 复活（dqb_free F3 排水 → dq_done wait → DQ_EPI(r)=0/1 双段 _store_dq_epi_tma_v12（t_dq[r]/dq_tmem_load/rank_dq_coordinates/s_dq_epi/epi TMA 三参 全休眠机器接线）→ release）；_zero_dq_v2 else 分支逐字不动；pds producer_tail 恢复 v12 S1 位次论证注释；
  - helpers：_issue_dq_block_v52 → _issue_dq_wave_v32 复活（v5.1b 逐字 + docstring 复活注记）；_offload_dq_block_v52 整函数删除（257 行，断言锚定式脚本切除，防 booby-trap）。
  py_compile OK。全文件退役符号 grep 清零（pipe_dq_evict/dq_evict_prod/evict_wait/evict_rel/t_dq_rot/dq_evict_c_layout/round_kd_evict/TMEM_DQ_SLOT/_issue_dq_block_v52/_offload_dq_block_v52——仅剩注释 2 处：12762 自注、16341 历史尾注 Z5 处理）。
[z5] Z5 清扫+账目+尾注完成：SUB_TILE_BOX 值锁别名退役（唯一消费者 offload fold 断言已死，注记留位）；DQ_EVICT_TILER 注释去 SUB_TILE_BOX 引用；slice-fold 注释群（13100/13428"dQ-eviction MMA"史实表述）判定仍准确不动（冻结毗邻区最小扰动）；class docstring v8/v12 血统注记为历史文档不动。span 名普查：28 名 + provenance 恰 1 mark = 29/29，名集与 v6 逐字相同（DQ_EPI(r) 复用、零增删）✓。V7 RESIDENT-DQ ADDENDUM 落笔文件尾（论题/TMEM 512 整/G5 驻留形/epilogue 复活/环 2 活性+STOP 门/等待图增量/协议账/payload 表/验收钩子 3 项；发射账 136/bundle vs v6 160 注记）。py_compile OK。终态 16,609 行。
[z6] 冻结区自证（脚本化 difflib，ast 函数级提取 + tokenize 剥注释，v6 a3dd79c ↔ v7 终态）：
  16 个受审 helper 中 15 个 IDENTICAL（剥注释后 0 行差）：_issue_dkv_block_pair_v51/_issue_score_pass_v6/_load_chase_piece_v32/_chase_slot_rows_v32/_kd_round_rows_v2/_gather_kdq_v8/_fill_kdq_pair_v8/_fill_kdq_pair_v2/_zero_dq_v2/_store_dq_from_tmem/_store_dq_epi_tma_v12/convert_canonical/_drain_dkv_v8/_split_wg_t1d_v2/_make_score_tmem_load；唯一例外 _drain_dkv_fused_v52 = docstring 4 行（4-deep→2-deep 口径），代码零差（comment-only，_load_chase_piece_v32 先例）。
  内核体 hunk 图全分类（diff 68 hunks 逐一归区）：GATHER 角色 0 hunk、W17 LOAD 角色 0 hunk、RELAY 角色 0 hunk（K 驻留+gather/strip 环/kdq gen/relay 四大冻结区一个字节未动）；MATH 角色 hunks 全部落在消费环之外（状态块 dq_done_state 新增 @13894a + 尾部 epilogue 区 @14441-14472）——消费环本体 13900-14440 零 hunk；其余 hunks 全部落在批准手术区（__call__ 断言/常量层/存储/序幕/REDUCE 角色/LEADER 角色/grads helper/G5 helper/尾注）。
[v7-final] 终报（驻留回归全案落地，py_compile 全过）：
  总 diff vs fork 基线（v6 终态 a3dd79c）= +457/−720 行（16,874 → 16,609，净 −265）。各区：Z1 常量/TMEM/断言/workspace 覆写删除 ~+70/−120；Z2 序幕（fragments 回 v12 形/管线换发/t_dq/army）~+60/−130；Z3 dkv 环 4→2（helper 签名+三调用点）~+25/−35；Z4 G5 驻留+epilogue+reduce 纯化+offload 删除 ~+190/−430（含 _offload_dq_block_v52 整函数 257 行）；Z5 清扫+尾注 ADDENDUM +103/−5。
  协议账（产=耗）：round 20=20/bundle（16 grads + 4 kdq——G5 由 v5.2 对双持回 v5.1b 单持逐 gen wait/release，FIFO 序不变）；dkv_done 16=16/bundle（8 融合对，环 2 相位：16 mod 2=0，槽律 p%2 静态）；s/dp 2=2、pds 4=4、kres 1=1、strip 4=4 恒等；dqb_free (N+1)=(N+1) 恒等；dq_done 1=1/tile（leader TAIL 群提交 ↔ math 尾部单耗）。G5 发射账：32 evict gemm/bundle → 4 驻留 wave MMA/bundle（K 链内藏，v5.1b 指令数恒等）。
  TMEM 映射回显：S[0,64)(S1@32) | dP[64,128)(DP1@96) | dkv 2×64 [128,256) | dQ0@256/dQ1@384 [256,512)；BUDGET=2*32+2*32+2*64+256=512 == TMEM_COLUMNS，__call__ 等式断言带双值回显。SMEM：mbar 账 −8 Int64（dkv 8→4、evict −4、dq_done 留存复活），≤215,040 上界式不动。
  冻结区自证：见 [z6]（15/16 IDENTICAL + 1 docstring-only；gather/W17/relay 零 hunk；math 消费环本体零 hunk；score 环 helper IDENTICAL）。
  span 名集：28 + provenance 恰 1 = 29/29，与 v6 逐字同名零增删；DQ_EPI(r) 语义回 TMA epilogue（payload r∈{0,1} 每 tile 一次，v12 表）；MAT_ACQ kdq 序号 20-23 不动；S_ISSUE 保 v6.1 拆分 4b+2t+half。
  自知风险清单（按危险度）：
   R1 TMEM 全图重排（S/dP 基址 32→0、dkv→128、dQ→256/384）——全部经类常量走静态槽址，冻结消费者均 self.* 取值，无硬编码残留（grep 核验）；若有漏网硬编码，correctness 门必抓。
   R2 pipe_dq_done/epilogue 复活机器在 v6 基座上首次合体（v5.1b r4 硬件背书 + v6 对 math_group/leader_group 零改动 ⇒ 低风险；症状=尾部挂死或 mdQ 尾块错数）。
   R3 累加初始化律（每 D-round 恰一次 False：bundle0-wave0）——v5.1b 逐字回植；若 trace 期对 loop_iter!=0 的 Boolean 选择有异议（无先例风险，v5.1b 同码已过硅）。
   R4 环 2 节流为性能险非正确性险——STOP 门已注册：实测 drain >5µs/对成为新 pacer 时上报（后备案=环 3 让 dQ 64 列）。
   R5 s_dq_epi 复用 round_buf a+b 连体——WAR 由 dq_done 群提交传递闭合（G5 = 末代 round 读者，v5.1b 合同逐字）；W17 在末 bundle 后不再填充，无新写者。
   R6 发射账 ruler 差异已在尾注注记（v6 160 为混合口径；v7 G5 按调用数 4/bundle 报账，不跨代直接比较）。
  py_compile：OK（逐区 + 终态复验）。不 commit（协调者权）；不投测试（proxy 归协调者）。验收门待硬件：correctness 4/4 → release 判决带 12-18ms → trace 四钩子（reduce 占用 89%→~50%、尾停摆 36µs→<8µs、grads 环 2 节流形态、S(1) 共驻衰减自然实验）。
[v7-r1] 硬件判决（协调者）：correctness 4/4 全过；release 21.943ms vs v6 35.548 = **−38%（战役首次真突破）**；周期 12.39µs/64kv（税后 43.5µs/bundle）。四钩子：尾停摆 36→12.8µs（offload 实心块消灭 ✓）；reduce w8 79%（其中 WAIT_dK≈ATOMIC≈2µs/对——drain 一半时间在等 grads MMA 的 TC 退队）；drain 节奏 4.95µs/对（贴 STOP 门 5 未破，环 2 站住）；S(1) 恒 3.10µs 且 **S(0) 0.94→2.08µs**（周期缩短后失去全场沉睡窗——共驻税命中双 pass）。**格局反转：案 1 的内核内 TC 有效吞吐税（孤立价 3-4×）从 3% 小案升格为主 pacer**——账面：杀税后周期 →15-20µs ⇒ e2e 8-10ms ≈ baseline 量级。下一步：NCU 地面真相轮（stall 分类/TC 利用率/L1TEX/时钟）在飞。
[v7-ncu] NCU 地面真相轮（--set full，41 replay，主 kernel 21.44ms 同量级）：
  (1) stall 分类：No-Eligible 84.09%、long_sb 55.5%、barrier 23.8%；math/MIO/TEX throttle ≈0；热点 PC = mbarrier TRYWAIT+NANOSLEEP 环、named-barrier 环、以及一个 LDG 轮询环（12.7% 全核采样，疑 gather/topk 索引）——聚合停顿被设计性等待主导，需按角色拆分判读。
  (2) **TC 实际活跃仅 11.5-17.6%**（≈纯 FLOPs 时间 5µs/43.5µs bundle）——"释放版 TC 钉死"论彻底埋葬；TC 跑起来就是全速，问题是喂不满。
  (3) 溢出实锤：96 reg/线程发射基线，local spill 15.28MB/kernel + SMEM spill 3.44MB，43-51% LDL/STL 来自 spill，L1 命中 11.7%；NCU 估算 local 修复 +24%、SMEM store 7.3-way 冲突（pds 存储）修复 +8%。但 spill 指令处仅 0.2% 停顿采样——真伤在依赖消费点（long_sb 自旋分支）。
  (4) **案1终审定罪：v11 时代配额表 × v7 新角色**：leader 家族 wg（w16-19）在 decrease(48) 桶（13555 行），v7 leader 工作量在 48 reg 下溢出 → 发射环 LDL 重载 → 共驻活跃时 L1 冲刷 → 100ns/条（沉睡时 spill 行驻 L1 → 28ns）。微基准七连无罪终解：小内核不溢出。约束：setmaxnreg warpgroup 粒度 + 8 倍数 + 池 640×96。DSL API 存在且在用（setmaxregister_increase/decrease）。
  (5) 时钟 1.837GHz 稳定（93.5% 额定）排除降频。
  → 配额网格编译扫描（每角色 spill 计数）+ 条件性 v7r2 已委托。
[ld-z0] V7.1-LD 开工（spec V7_1_LD_SPEC.md 29 行全读；V7 log 46 行/V6 log 49 行全读；铁约束入脑：存储侧优先、slab 消费者零触碰、字节语义不变、无解 STOP）。侦察计划：v7_full_source.csv 过滤 Shared+Store 行按 L1 Conflicts Shared N-Way 排序 → SASS 邻域特征（F2FP/STSM/STS 宽度）→ 对上 math 发布块（publish domain (16,2) 列箱 / J-mode store tiles / v9_3 stmatrix）。证据行号先入日志再动刀。
[ld-z1] NCU 侦察判决（证据行号入册）：
  (1) "7.3-way"口径破译：details.txt L5188-5192 = op_st 总 wavefronts 61,688,466 ÷ "8,413,184 store requests"（该分母 = STS 族 8,388,608 STS.U16 + 16,384 STS + 8,192 STS.U8，恰不含 STSM）= 7.33——NCU 叙事把 STSM 的 wavefronts 算进分子却不算请求进分母，7.3 本身是口径混装，不是任何单条指令的真实 way 数。
  (2) 真冲突计数：l1tex__data_bank_conflicts_pipe_lsu_mem_shared_op_st.sum = 11,307,666（details L4762）；对账：HW op_st wavefronts 61.69M − source CSV 全店 nominal 50.36M = 11.33M ≈ 冲突数（差 24,576）⇒ CSV 每指令 wavefronts 列是理想值，冲突未做逐指令归因（L1 Wavefronts Shared Excessive 全 0，CSV 11,632 行核验）。
  (3) 店面清单（source CSV Shared+Store 全体）：STSM.16.M88.4 ×24 站（csv 行 2992-3343，16 无谓词 + 8 @!P0/@!P1，各 524,288 exec，共 12,582,912 exec = smsp__inst_executed_op_shared_stsm 全额，nominal 41.94M wf）；STS.U16 ×256 站（各 32,768 exec，共 8,388,608）；STS ×3 + STS.U8 ×1（微量）。"N-Way"列 4/2/1 = 每请求 nominal wavefronts（512B/256B/≤128B），非实测冲突 way——32 行 flag=4 全是无谓词 STSM+LDGSTS.128，NCU source 页已知怪癖。
  (4) 待裁决：11.3M 冲突落在 STSM 族还是 STS.U16 族，出口导出无归因 ⇒ 转入源码 bank 代数定罪（STSM [Rxx]/[Rxx+0x800] 双站结构 + 0x800=2048B 步距已记）。
[ld-z2] 店主指认完成（源码 ↔ SASS 对号）：
  (1) STSM ×24 站 = math P/dS/dq_b 发布机全额：8 无谓词 = P 发布（4 slice × 每 warp 2 条 [Rxx]/[Rxx+0x800]，0x800=2048B=16 行）；8 无谓词 = dS 发布；8 谓词 @!P0/@!P1 = dq_b own-image 发布（源 14280 `if Int32(sub_tile)==rank` 运行时 rank 门 → 谓词化，wf/exec=2.0 即半程写入自证）。发射点 = 14184/14268/14281 的 cute.copy(tiled_copy_r2s,...)，目标 = score_store_domain(12891) @ SW128B（score_store_layout.inner==dkv_a/dq_b inner 断言 12823-12830）。
  (2) STS.U16 ×256 站 = math dQ 尾部 epilogue（DQ_EPI）转换店：SASS 特征 F2FP.BF16.F32.PACK_AB + PRMT + LDL.S16(spill 回读) + VIADD/SHF>>3/LOP3(0x70)（=SW128 XOR 地址代数），目标偏移 0x21a00-0x24380 = round_buf_a 区（= s_dq_epi 别名视图）；两个对称块（基址 R0 / R36）= DQ_EPI(r) r∈{0,1} 双段；exec 32,768/站 = STSM 站的 1/16 = 每 tile 一次（16 bundle/tile 律）✓ 铁证：这是 _store_dq_epi_tma_v12（v5.1b 冻结 helper），不是 kdq fill。
  (3) 归因僵局的代数：真冲突 11,307,666 无逐指令归因（CSV wavefronts 列 = 名义值；"N-Way"列= 名义 wf/请求：STSM 无谓词 4、谓词 2、STS.U16 1、LDGSTS.128 4——全查表证实）。守恒方程：10,485,760·a(STSM 有效条) + 8,388,608·b(STS.U16 条) = 11,307,666−24,576(STS/STS.U8 2-way 小额)。a=0 ⇒ b≈1.345；b=0 ⇒ a≈1.076。
  (4) STSM 发布 bank 代数（初判无罪）：stmatrix.x4 每相位 8 lane = 1 矩阵 = 8 连续行 × 16B @ 同列窗；SW128 chunk' = chunk ^ (row%8)，行基 8 对齐 ⇒ 8 行 XOR 出 8 个互异 16B bank-group ⇒ 4 相位全零冲突。J 箱列偏移（j*32B/box+64B）只平移 chunk 基值，不破互异性。待终验：dq epilogue STS.U16 的 TV 映射精确计算（若坐实 ~2.35-way，spec 前提"冲突主要来自 P/dS 发布"被证伪 → STOP 汇报线）。
[ld-z3] .ncu-rep 直读（Mac Nsight Compute 2026.2.1 ncu_report python 绑定，v7_full.ncu-rep 199MB 解压验读）：per-SASS 实例级金标确认——derived__memory_l1_wavefronts_shared_excessive 全 740 实例 = 0；memory_l1_wavefronts_shared == _ideal 逐实例相等（69,537,792 总额）；derived__memory_l1_conflicts_shared_nway 总和 459 = 名义 wf/请求表（非实测冲突）。警示：这些 per-SASS wavefronts 数值全部与"访问字节量/128B×谓词掩码"纯尺寸模型一致（@!P0 STSM 恰 2.0 = 50% 掩码），无法证明做过真实地址 bank 仿真 ⇒ 出口层面归因彻底不可得，判决只能靠源码 TV 代数。当前代数状态：STSM 发布（per-matrix 8-lane 相位 × SW128 chunk^row%8 → 每相位 8 bank-group 互异）与 dq-epi STS.U16（lane=DP=d 连续 → 同 head 行 64B 连续段）双双初判无罪——若坐实，11.3M op_st 冲突必然来自地址模式之外（跨管争用：TC pipe shared wavefronts 771.75M = LSU store 流量 12.5×，共驻 bank 争用率账面 13-18% ≈ 实测 18.33% 冲突率）→ 存储侧无刀可下 → STOP 线。剩余工作：用 DSL 源码 TV layout 把两台机器的 lane→addr 映射从假设升级为推导。
[ld-z4] 终判代数（三重锁死，spec 约束 1 的 STOP 分支触发）：
  (A) STSM 发布模式无冲突（强制性证明，非假设）：发布像 [64 kv 行 × 64 头列] bf16，行 128B，SW128（inner==dkv_a/dq_b 断言）；chunk'(r)=k⊕(r%8)。stmatrix.x4 每相位 8 lane=1 矩阵=8 行×16B；域行模 (8,8):(64,512) 线性（r×64el），矩阵 8 行连续且 8 对齐 ⇒ chunk' 是 {0..7} 双射 ⇒ 任意列箱 k（(16,2) 箱给 k∈{2j,2j+1,4+2j,5+2j}，J/box 只平移 k）零冲突。反设穷举被计数器否决：行跨 2 ⇒ 全相位 2-way = +4/条 = 42M ≫ 11.3M；跨相位配组 ⇒ 同为 +4/条级；即"若 STSM 有模式冲突，量级必为 42M+"，实测 11.3M ⇒ STSM 模式冲突 = 0 被数据强制。
  (B) dq-epi STS.U16 模式无冲突（同为强制）：s_dq_epi [128 头 × 128 d] bf16 行 256B SW128；T2R lane↔DP=d ⇒ 每条 STS 32 lane = 同 head 行 d 连续 64B 段（相邻 lane 2B 并字）⇒ 16 bank 互异零冲突。反设（lane↔head 转置映射）⇒ 32 lane 同列跨 256B 行 → chunk' 只取 4 值 → 8-way = +7/条 = 58.7M ≫ 11.3M，否决。任何 16dp 变体给两个 32B 段，chunk 差 4 仍互异。
  (C) 冲突真身 = 时域跨管争用，非地址模式：守恒帐 11.28M 无法由任何 (A)(B) 合法模式组合凑出（可行模式增量均为 {0,+4,+7}/条 的整数簇，与 1.076/1.345 连续值矛盾）；旁证 TC pipe shared wavefronts 771.75M = LSU 全部流量的 4×（l1tex 忙约 13.4%），store 冲突率 18.3% / load 6.5% / ldgsts 0.6% 与各家族跟 UMMA 读窗的时间重叠度排序一致。共驻 bank 争用不依赖存储地址序 ⇒ 重排 TV/tile 顺序/stmatrix 挡位/打包宽度全部无靶可打。
  (D) 奖金核算（即使全额消除亦无肉）：11,307,666 冲突周期 ÷ (8192 CTA × 16 bundle) = 86 cycle/bundle/CTA ≈ 47ns @1.837GHz，对 24.8µs bundle = 0.19%（全串行化上界 ~0.4%）；NCU "8.159%" 是 l1tex 子系统局部公式估算，不是 e2e——"7.3-way"叙事本身是 STSM wavefronts 进分子/STS 请求做分母的口径嵌合体（[ld-z1](1)）。
[ld-final] STOP 终报（存储侧无解，按 spec 约束 1 第二分支收刀）：
  改动 diff：内核 dsa_bwd_sm100_2cta_v7.py 零字节未动（git status 自证，py_compile OK）；本文件日志 6 条为唯一改动。冻结区全体 IDENTICAL（无编辑=平凡自证）。
  结论链：① NCU 7.3-way = 口径嵌合，真冲突 11.3M op_st；② 逐指令归因在 --set full 出口不存在（excessive 全 0 = 尺寸/掩码模型，非 bank 仿真）；③ 两台店（math P/dS/dq_b STSM 发布 ×24 站、math dQ 尾部 epilogue STS.U16 ×256 站）模式层面双双无冲突——由 SW128 XOR 双射代数 + 计数器量级否决法联合强制；④ 11.3M 为 TC/UMMA 共驻时域 bank 争用，地址序不变量，存储侧（TV 重排/挡位/打包宽度）数学上无靶；⑤ 奖金上界 0.2-0.4% e2e，远低于 NCU 局部估算的 8%。
  风险清单：R1 若 NCU 未来版本给出逐指令实测冲突且落在 STSM（概率低：与 (A) 反设否决矛盾），需重开——复核路径：~/proxy/dsa-ncu-v7-1785872244/v7_full.ncu-rep.gz + Mac Nsight Compute 2026.2.1 的 ncu_report python 绑定（本轮已验通）；R2 时域争用的可攻面在调度层（发布窗 vs UMMA 读窗错峰、或 L-C math 双 warpgroup 化顺带改变重叠形态）= v7.2+ 级，需另批 spec；R3 顺手发现（不动刀）：dQ epilogue 的 F2FP 源自 LDL.S16 spill 回读——案 1（local spill，NCU 估 24%）在 epilogue 内的具体形态，属结构路线弹药。
  建议下一杠杆：按 v7-r2 序列跳过 L-D，直上 L-C math 双 warpgroup 化（12→6µs 主路），L-D 的 8% 估算作废。
[v7-r2+配额网格终审] (1) 配额路线证伪：6 配置网格（B200 编译+SASS spill 计数），现状 A（leader48）全指标最优——任何加大 leader 配额的配置 leader spill 反增（335→426-446，ptxas 全局分配耦合），STACK 亦增。案 1 修复只剩结构路线。(2) v7r2 复测 21.97ms（稳定）。(3) 12.7% 热环判读：融合 drain 的 kv_index 解码 + reduce 组内 barrier 停靠 = 设计性 WAIT_dK，非 bug。(4) 周期残余解剖（release ~24.8µs/bundle）：math 串行链（softmax/pds ~3µs×4 slice ≈12-14µs）= grads 相位深层 pacer；leader 带税发射 5-7µs；尾部 K 翻转+credits ~7µs。(5) 下一杠杆序：L-D pds STS 7.3-way bank 冲突 swizzle（NCU 估 8%，直击 math 关键链，但触冻结 slab 消费者需正规 spec）→ L-C math 双 warpgroup 化（12→6µs，v8 级手术，通往 baseline 的主路）→ L-B D128 肥化（S 面条数再减半）。
[v8-LE-r1 判决·回滚] correctness 4/4；资源大胜（math spill 156/84→12/3 −94%、whole 609→395）但 release 22.27ms vs 21.97 = **+1.3% 回退** ⇒ 按 e2e 铁律 git revert。证据价值：(1) spill 无害论坐实（225 条 math spill 消失而 e2e 不动，与停顿采样 0.2% 互证）——案 1 的 spill×L1 理论降级为"相关非因果"；(2) math T2R 延迟被藏住仍不动 e2e ⇒ math 链有 slack、非关键路径；(3) leader 发射节奏仍为首嫌，但机制既非配额（网格证伪）亦非 spill（本轮旁证）——"共驻活跃 100ns/条"的物理载体悬案重开。杠杆队列余项：L-B D128 肥化（砍 leader 半数 score 穿越，但需解冻 gather/K 驻留 = 全文件最高危区，须正规战役）；leader 结构瘦身（靶点不明，暂缓）。e2e 台账：v7 21.94ms 为当前最优战果（baseline 8.28-8.42 浮动，v17a 11.51）。
[lb-recon-z1] V9-LB 侦察轮开工（只读，零修改权；spec 草案 V9_LB_D128_SPEC_DRAFT.md 27 行全读）。取证面：v7 HEAD 24caeb3 源码（K 驻留布局 11787-11813/12161-12166/12734-12737、填充机 1192-1453/12217-12236/13583-13714、score 环 15201-15362、kdq 面 12392-12540/12751-12764）+ DSL 布局函数源（cutlass/utils/blackwell_helpers.py: make_smem_layout_a/b + tcgen05/helpers.py: make_smem_layout_atom/tile_to_mma_shape，本地 DKG 拷贝）。
[lb-recon-z2] 现状布局精确提取：score_a_layout_staged = make_smem_layout_a(score_tiled_mma, (128,64,64), bf16, 8)（源 603-608，score_tiler@520 = (H_TILE_CLUSTER, SCORE_MMA_N, K_CHUNK)）。DSL 代数：K-major ⇒ K_SW128 原子 = S<3,4,3> ∘ (8,64):(64,1)（major 64el=128B, 1024bit 整除，helpers.py:95-97/118-120），tile_to_mma_shape order=(1,2,3)（blackwell_helpers.py:824——rest_M 最快、rest_K 次之、stage 最慢；MN-major (2,1,3) "rest_k fastest" 有 v5.2 硬件背书注释@540-545 反证排序语义）。展开：offset_el(p,m,d) = p·4096 + (m//8)·512 + (m%8)·64 + d（p=piece∈[0,8), d∈[0,64)），stage 步长 4096el=8192B 连续紧凑——与源码注释"8,192 B contiguous blocks"@11793 及 score_a_stage_bytes=8192B 互证。
[lb-recon-z3] D128 目标布局推导：make_smem_layout_a(同 MMA, (128,64,128), bf16, 4)——K=128el=2048bit 仍整除 1024 ⇒ 同一 K_SW128 原子、同一 swizzle；per-stage (M64,K128)：rest_M=8(步长512)、rest_K=2(步长4096)、stage 步长 8192el=16384B。展开：offset_el_new(s,m,d) = s·8192 + (d//64)·4096 + (m//8)·512 + (m%8)·64 + (d%64)。
  **字节恒等式（成立，精确零余项）**：代入 p = 2s + d//64、d_old = d%64：offset_el_old = (2s+d//64)·4096 + (m//8)·512 + (m%8)·64 + (d%64) ≡ offset_el_new。∀(m,D)∈[0,64)×[0,512)：addr_old(D//64, m, D%64) = addr_new(D//128, m, D%128)。swizzle 相容性：两布局的原子实例均落 512el(1024B) 对齐槽位、原子内坐标 (m%8, d%64) 逐点相同 ⇒ XOR 物理字节逐一相同。即 8×D64 与 4×D128 两个 auto 布局是**同一字节函数**（新 stage s = 旧 piece 对 {2s,2s+1} 原位串联，+8192B 处即第二半）——"重装箱"物理上是空操作，纯视图别名。
  B 侧同构：score_b (32,64,8st)→(32,128,4st)：offset_new(s,n,d) = s·4096+(d//64)·2048+(n//8)·512+(n%8)·64+(d%64) ≡ offset_old(2s+d//64, n, d%64)；对 v6 strip 恒等式（stage d·8192+kb·2048）：新 B 窗 s = strip(d=s//2, kb 对{2(s%2),2(s%2)+1})，D128 窗永不跨 strip stage（2:1 天然对齐坐实，spec 预判成立）；piece==SCORE_D_PIECES//2 的早释放边界@15288 自适应落在 strip 段界。
  硬件血统三重背书：① v0 基类**已在生产跑过 (128,64,128) 4-stage K-major A**（K_CHUNK=128/SCORE_A_STAGES=4@167-180 + _issue_score_chunks@1168-1190，v0/v1 bring-up 硅上 correctness 过）——目标布局非新事物；② v3.2-v5 面板 [32×512] = 8 连续 [32×64] 窗读的 intra-stage 块序恒等（@708-724 注明由 correctness 门裁决且历代全过）= rest 排序（minor 内/restK 外）的硅证；③ v6 修正案 #1 R-B s·2048=(s//4)·8192+(s%4)·2048 同代数类，v7-r1 correctness 4/4 现役背书。
[lb-recon-z4] 填充机与消费面受扰审计：
  (1) **LOAD_K 无 TMA**——K 驻留填充是 cp.async 128-bit 逐行 gather（_copy_sparse_k_d128_row@1192-1235，16B/thread/段），spec 危险声明中"D64→D128 TMA box 合法性"命题落空（不存在该 box）；strip TMA [h32×D256] 与 K_CHUNK 无耦合，不动。
  (2) 填充机全体 K_CHUNK 参数化：_copy_sparse_k_d128_row（宽度 K_CHUNK、tile 环 K_CHUNK//64→2、d_offset=piece*K_CHUNK→{0,128,256,384}）、_zero_sparse_k_d128_row、_load_chase_piece_v32（行程与 piece 无关）、_chase_slot_rows_v32（composition (64,K_CHUNK) 的 flat 坐标链 64×16×restK 在 D128 下仍精确覆盖 canonical (M,K) 空间——逐模校验过）：**零代码触碰**。唯一代码改动 = kres_rows 8 项元组@13606-13615 → 4 项（机械删行）；cp_async commit 组 8→4、wait_group(0) 语义不变；pipe_kres 为计数制（256 线程 arrive，无 expect_tx 字节账）。
  (3) kdq 面（G5-A）**结构性解耦已在 v3.2 埋好**：_copy_sparse_k_row_v32 注明"names its width explicitly instead of inheriting K_CHUNK"@1251-1256，_fill_kdq_pair_v8 全部显式传 D_TILE_CTA；round_kd 视图源自 dq_a_layout_staged（DQ_MMA_TILER 派生）——零触碰、零索引变换。
  (4) score 环消费侧：_issue_score_pass_v6 的 piece 环 = SCORE_D_PIECES、k_blocks = size(mode=[2]) 动态、半界 = SCORE_D_PIECES//2、ACCUMULATE 首块翻转按 piece==0 守恒——**常量翻转后全自适应，零代码触碰**；strip 协议 4=4、S_ISSUE payload 4b+2t+half、pds/math/grads/relay 全平面零涉。
  (5) 断言全数自动成立：cosize 32768 恒等（=SCORE_A_MAX_ELEMENTS）、score_b cosize 16384 恒等贴 strip 缓冲、inner swizzle 同族（696/701-724/752/755/11274/12102）。
  (6) spec 预检勘误一处：**"MAT_ACQ 序号表 wide gen 16→8 需重编"系误判**——MAT_ACQ 序号 wide 4-19 是 16 个 grads round gens（[h64×own-D64] Q/dO 流，锚 DKV_D_ROUNDS/D_TILE_CTA），与 score piece 数无关；D128 下 W17 gen 表 24 项（strip 0-3/wide 4-19/kdq 20-23）逐字不变，定稿应剔除该项。
[lb-recon-final] 终裁：**GO（且走捷径形态：字节不动点，零重装箱）**——但附一条性能前提警示（不阻塞正确性）。
  恒等式判决：成立（z3 精确代数 + 三重硅背书）。字节层风险 = 零；描述符全程 canonical auto 布局（R-B 纪律，零新风险类）；SMEM/TMEM/发射结构零变，可无痛回滚。
  手术区清单（GO 情形，按危险度升序）：
   S1 常量层（~6 行）：K_CHUNK 64→128@11787、K_CHUNKS 8→4@11788、SCORE_A_STAGES 8→4@11797、SCORE_B_STAGES 8→4@11811、SCORE_D_PIECES 8→4@11754、SCORE_MMA_TILER 文档常量 (128,64,64)→(128,64,128)@11752（全文件唯一非注释引用即定义处）。宿主 __call__ 经 score_tiler@520 全自动。
   S2 gather 冻结区唯一代码触碰（~4 行删除）：kres_rows 元组 8→4 项@13606-13615。填充 helper 全家零触碰（K_CHUNK 参数化自洽，[z6] 式剥注释自证可期）。
   S3 注释/账目（doc-only）：11790-11810 驻留注、12161-12166 存储注、15220-15257 score pass docstring、尾注 ADDENDUM；MAT_ACQ 表不动（z4(6) 勘误）。
   S4 验收：py_compile → correctness 4/4（字节恒等式的硅裁决）→ release 判决。
  性能前提警示（建造者与用户须知，唯一实质风险）：发射账在**任何含 k_block 因子的口径下 D128 恒等不变**——per-plane gemm 调用 = 2 pass × pieces × k_blocks = 2×8×4 = 2×4×8 = 64（UMMA 指令数同理守恒）；D128 砍半的只有 piece 窗跃迁数（per-plane 16→8/bundle）。故 −3~−4µs/bundle 论题成立当且仅当 ~100ns 税附着于 piece 窗级（微基准 (128,64,64)→(128,64,128) 单价 +13ns 的形态支持此判——若税是 per-call/per-instruction，K 翻倍应近乎翻价而非 +13%）；但 v8-LE-r1 已把税的物理载体重开为悬案，且微基准的共驻 regime 迁移性未验。若税实为 per-call，V9-LB 收益 ≈ 0（正确性仍无损）。附：若建造者为强制窗级摊税改单调用肥 gemm（dispatch [5] K-rest 模式），ACCUMULATE 字段是 per-call 的——piece 0 首 k_block 的 False→True 翻转必须保持拆分或先探 DSL 内部翻转语义；现行 per-k_block 环形态零此险。
  NO-GO 卡点：无。备选捷径已升格为主案（不是"消费侧 D128 视图别名而物理序不动"，而是更强命题：两布局本就是同一物理序——别名与重装箱在此恒等式下是同一件事）。
  本轮改动：内核零字节（只读侦察）；本文件 5 条日志为唯一写入。
[v9-LB 落刀·协调者] 按侦察 GO 捷径实施：S1 六常量（SCORE_MMA_TILER (128,64,64)→(128,64,128)、SCORE_D_PIECES 8→4、K_CHUNK 64→128、K_CHUNKS 8→4、SCORE_A/B_STAGES 8→4）+ S2 kres_rows 元组 8→4 项——字节不动点恒等式（[lb-recon-z3]）下重装箱为物理空操作，gather/chase/kdq/score 环全零代码触碰。py_compile OK。判别设计：本轮同时裁决案 1 税载体——窗级税 ⇒ e2e −3~4µs/bundle；调用级税 ⇒ 零收益（无痛回滚）。
[v9-LB-r1 判决·保留] correctness 4/4（字节不动点恒等式硅裁决通过）；release 21.040ms vs 21.974 = **−4.1%（新最优）**。税载体判别：混合型——窗跃迁减半兑现 ~1µs/bundle（约税的 1/4 附着窗级），其余为调用级（每 cute.gemm 调用固有）。e2e 台账：35.65 → v7 21.94 → **v9-LB 21.04ms**（−41% 累计；baseline ~8.3）。案 1 残余方向：调用级税的 SASS 解剖（每调用指令数/描述符构建开销）——NCU source 转储可直接数。
[l2-recon-final] L2（K 驻留双缓冲）纸面侦察终裁（只读轮，内核零字节；v7r1_iket.json 12 个边界逐 span 填图为证）：**全量 DB = NO-GO（字节死刑）；部分 DB n=1 = 技术可入账但被支配弃案；改判 L2-lite（gather 流重排，0 字节、协议零改）为主案——trace 翻案：LOAD_K 钉在边界不是单缓冲 WAR 所迫，而是 gather warp 指令流位置所迫。**
  (1) SMEM 字节账（SharedStorageV2@12114-12214 逐项）：mbar 头 364B（s_done 32/dp_done 32/kres 16/strip 32/round 32/pds 32/dkv_done 32/dq_done 16/stat_tma 16/stat_ready 16/landing 16/relay 16/round_tma 16/pds_ready 8/dqb 16/dqb_free 16/khot 8/tmem_dealloc 8/holding 4）pad→1,024；缓冲 stationary_q 32,768@12158 + stationary_do 32,768@12162 + score_kv 65,536@12168 + round_buf_a/b 2×16,384@12172/12176 + p_slab/ds_slab 2×16,384@12182/12188 + dq_b 16,384@12196 + stats 1,024@12201 = 214,016；总 **215,040 == 断言上界@12211（贴满零余）**；cap = MAX_SMEM_BYTES 232,448@227 ⇒ 净余 **17,408**。全量影子 +65,536 → 280,576 超 48,128（纯缓冲 279,552 已超 cap）——且无 48KB 级可回收项：[z2] 删的是 dq_b 双像"视图/断言"，8,192-el 缓冲本体存活（dqb 门在用），字节零节余，任务背景的节余预设不成立。部分影子 +16,384×n：n=1 → 231,424 ≤ cap（余 1,024）✓；n≥2 → 247,808 不入账。
  (2) trace 定罪（v7r1 CTA0，b=1..12 铁周期，43.5µs/bundle）：边界链 = pair7(b) MMA 退队(WAIT_dK 8b+7 end) → +0.35µs kdq r0(b) 填尾（ring-2 信用 g16←g14 释放 = pair7 退队，结构性钉死在边界）→ **LOAD_K(b+1) 6.0-6.6µs 全额在链** → +2.0µs kdq r1(b) 填+ROUTE_K(2b+1) 会合（g18/g19←G5 r0(b) 退队）→ +0.8µs W17 LOAD_QDO strip（被 ROUTE_K 压在 W17 串行流之后）→ +0.5µs S_ISSUE(4b+4) → +3.0µs S(0)(b+1) 执行毕(WAIT_S 8b+8 end)；合计 ~12.7µs ≈ v7r1 报的 12.8µs 尾停摆（leader 末 dVdK→S_ISSUE 实测 12.3µs）。**翻案证据**：kres 1-stage 的 WAR 门（release@14622，tcgen05 track S(0)+S(1) 读）mid-bundle 即开——bundle3 实测 WAIT_S pv28 end 164.7 / pair0(3) 退队 166.2 ⇒ release ~166，距边界 191.5 尚有 **25µs**；LOAD_K(4) 却在 192.1 才起跑，因 gather 迭代序 = [LOAD_K(b); kdq r1(b-1); kdq r0(b)]@13627-13698，LOAD_K(b+1) 被排在 kdq r0(b)（边界钉死）之后。v5.1b 注释@13661"K prefetch never waits the late r1 credits"的取舍在 v7 驻留 G5+环 2 时代已反转失效。
  (3) 主案 L2-lite（建议下一落刀）：gather 迭代重排 [kdq r1(b-1); LOAD_K(b+1); kdq r0(b)] + 序幕剥离 [acquire; LOAD_K(0); commit] + 末迭代剥离（无 LOAD_K；建议尾迭代剥离而非运行时 if 包 state.advance，保直线体静态纪律）。kres 管线/mbar/wait@14577/release@14622 逐字不动（acquire 只是更早返回，1-stage 相位交替天然支持）；kdq 命名屏障全局序 r0(b),r1(b),… 逐字不变（LOAD_K 无 rendezvous）；W17/relay/leader/math 全平面零触碰。手术区仅 gather 角色体 13617-13716：重排 ~30 行 + 序幕/尾迭代剥离 ~25 行（断言锚定式复制防 booby-trap，或提 helper）+ LOAD_K payload 语义移位注记。门核验：重排后 acquire（~mid-bundle 166）早于 kdq r0(b) 自身信用（~191），不引入新停：安全。风险层级：R-低=机械重排相位错 correctness 门必抓；R-中=剥离复制体走样；R-perf=fill 移入 TC 活跃期与 MAT_QDO TMA/strip 争带宽（NCU l1tex 忙 13.4%，账面有闲）。
  (4) 若坚持真 DB 的手术面（存档备查，均判弃）：协议层 kres_mbars 2→4@12121、create num_stages 1→2@13273-13280、gather producer state@13603-13606、leader kres_cons@14496-14499（acquire@13627/commit@13655/tail@13715/wait/release 位点本体不改，state.advance 承相位）；填充机 kres_rows 元组 ×2@13611-13616 + stage 交替——bundle parity 是运行时值，fix-r7 静态纪律下元组 Python-int 索引不可用（dkv p%2 先例不迁移：彼为 constexpr 环内相位），须 staged if 双份填环或 Constexpr buf 参数双调用（lesson #14 状态回传风险）；消费侧注入点 = score_k_fragment/dp_k_fragment@13188-13189（k_chase@12739 扩 stage 模式）+ gemm 操作数窗@15317/15338（piece→piece+4·buf）或 _issue_score_pass_v6 加 Constexpr buf_base 于调用点 14589-14611 分支双调；**kdq 面零注入**——G5 读 round_kd（round_buf via dq_a_layout_staged@12756-12765），K 引用由 _gather_kdq_v8 独立自 GMEM 再采，v3.2 已解耦（spec 预设的第 4 注入点落空，与 [lb-recon-z4](3) 同判）。n=1 部分影子还额外需 pipe_kres0 第二门 + score pass 内 piece0 早释放（strip 早释先例@15301）+ parity 注入 = 中高危面，护住的只有 fill 的 1/4 ≈ 1.6µs 上界——被 L2-lite 完全支配。
  (5) 收益预估复核：L2-lite 链上摘除 6.0-6.6µs，残链（credits/翻转序）= kdq r0 尾 0.35 + G5 r0 退队 ~1.5 + kdq r1 填 1.8 + 会合 0.1 + strip 1.5 + S_ISSUE 0.5 + S(0) 执行 2.9 ≈ **7µs**（trace 制）⇒ 每 bundle 省 ~5-6µs（trace）/ 折 release 制 **−3~5µs/bundle**（v7-r2 尾账"K 翻转+credits ~7µs"中 fill 占大头；v9-LB 已验尾结构存续）⇒ e2e 21.04 → ~18-19.5ms 量级。真 DB 相对 L2-lite 的增量收益 = 0（两案 fill 均已离链；影子只把可起跑点从 mid-bundle 提到 bundle 头，窗 25µs ≫ 6.6µs fill，无边际）。残链 7µs 是下一战役（round ring 2→3 或 r1 预填 = 解冻 kdq 机器，须另批 spec）。
  本轮改动：内核零字节（git 台账 49e2f2d 未动）；本文件 1 条日志为唯一写入。
[l2l-z0] L2-lite 落刀开工（协调者解除只读，范围=主案 (3)）。手术计划：gather 角色体 13617-13716 单区——序幕剥离 [acquire; fill(0)=bundle tile_count-1; commit]（断言锚定式逐字复制填充块）→ 稳态环 range(tile_count-1) 体序 [r1(b-1) 守卫如旧; acquire; fill(i+1)=bundle_idx-1; commit; r0(b)]（直线体，producer advance 不入运行时 if）→ 末迭代剥离 [r1(1) 以 tile_count>1 守卫（等价原 loop_iter>0 守卫在末迭代的取值）; r0(0)] → 尾声 r1(0)+producer_tail 逐字不动。核验表预置：kdq 全局序 r0(b0),r1(b0),…,r0(bN-1),r1(bN-1) 逐字同序；fill 数 1+(N-1)=N、acquire/commit/advance 数=N=leader wait/release 数；LOAD_K payload=服务迭代号（集合 0..N-1 不变，仅墙钟提前一 bundle 落点）。铁约束重申：kres 管线创建@13273/mbar@12121/leader wait@14577/release@14622 零触碰；W17/relay/leader/math 零触碰。
[l2l-z1] 手术完成，逐区 py_compile OK。落点（术后行号）：角色头序注释改口 @13594-13601；序幕剥离 [acquire@13649; fill(0)=bundle tile_count-1, payload Int32(0) @13650-13676; commit@13674]（填充块断言锚定式逐字复制：piece 环/_load_chase_piece_v32 十二参/commit_group/wait_group(0)/fence 逐行同源）；稳态环 cutlass.range(tile_count-1) @13685：[r1(b-1) 守卫 loop_iter>0 @13695（十四参逐字不动）; acquire@13719; fill(i+1)=bundle_idx-1, payload loop_iter+1 @13720-13752; commit@13747; r0(b) @13757（逐字不动）]——直线体，producer advance 无运行时 if 包裹；末迭代剥离 @13777-13807：[r1(1) 守卫 tile_count>1（= 原 loop_iter>0 在 loop_iter=N-1 的取值，实参 Int32(1),Int32(1)）; r0(0) 实参 Int32(0),Int32(0)]；尾声 r1(0) @13809 + producer_tail@13824 逐字不动；尾注 L2-LITE ADDENDUM 落笔文件尾（payload 语义不变/墙钟提前一 bundle 注记，协调者令 3）。
[l2l-final] L2-lite 终报（py_compile 全过，不 commit、不投测试）：
  diff：+164/−39（16,612 → 16,737，净 +125，其中注释/尾注约 +70）；hunk 全落 gather 角色体 13588-13825 + 文件尾 ADDENDUM（comment-only）。铁约束自证：git diff 中 pipe_kres 相关 +行仅 producer_acquire/producer_commit（序幕复制体）；consumer_wait/consumer_release/管线创建/kres_mbars 零出现 = leader 侧与协议层零触碰（wait/release 现位 14686/14747，纯行号漂移）；W17/relay/math/reduce 角色体零 hunk。
  等价性核验（符号仿真 N=1/2/3/16 全过）：kdq (bundle,round) 全局序新旧逐项相等（含 N=1 退化：无稳态环，剥离 r1 守卫正确跳过）；fill (bundle,payload) 集合恒等、fill 数 = acquire = commit = advance = N = leader wait/release 数；LOAD_K(i) payload 语义不变（= 服务迭代号），站点 1→2（序幕+稳态，S_ISSUE 双站先例），29/29 span 名集零增删。
  门核验复述（协调者令 4）：新体序 [r1(b-1); acquire+fill(i+1)+commit; r0(b)] 的三门时序链——r1(b-1) 信用（g18/g19 ← G5 r0(b-1) MMA 退队，边界+~2µs）**早于** kres empty 边（leader release@14747 tcgen05 track S(0)+S(1)(b) 读，mid-bundle ~+18µs，v7r1 实测距边界 25µs）**早于** r0(b) 环信用（g16/g17 ← g14/g15 释放 = grads pair-7(b) MMA 退队，≈ 下一边界）。故 (a) r1 前置永不等 fill；(b) fill 的 acquire 阻塞解除严格先于 r0 自身信用到达——中段 [acquire; fill; commit] 不可能迟滞任一侧 rendezvous；(c) 1-stage 相位代数不变：P.acq(k)←C.rel(k-1) 世代配对与旧流逐代相同，仅墙钟提前（序幕吃 init 信用 = 原 bundle-0 行为）。死锁审计：gather 阻塞于 acquire 时 leader 不依赖 gather（score(0/1)(b) 的 K/strip 均已就位），无回边；kdq_barrier 双侧 arrive_and_wait 对称，到达序无关。
  验收钩子（待硬件）：correctness 4/4（kdq 序/相位的硅裁决）→ release 判决（预估 −3~5µs/bundle，21.04 → ~18-19.5ms 带）→ trace：边界链 pair7(b)→S_ISSUE(4b+4) 摆脱 6.0-6.6µs LOAD_K 环节（12.7 → ~7µs 残链 = G5 r0 退队→kdq r1 填→W17 strip→S_ISSUE）；LOAD_K span 应现身 mid-bundle（与 S_ISSUE/dVdK 重叠）而非边界。风险与 [l2-recon-final](3) 同：R-低 机械相位、R-中 复制体走样（已逐行核对）、R-perf 带宽争用（NCU l1tex 13.4% 有闲）。
[l2lite-r1 判决·保留] correctness 4/4（重排等价性硅裁决）；release **19.093ms** vs 21.040 = −9.3%，正中预估带（18-19.5）。LOAD_K 摘链兑现。e2e 台账：35.65 → v7 21.94 → v9-LB 21.04 → **L2-lite 19.09ms（累计 −46.4%）**；baseline 8.33（2.29×）。残余账目：尾部残链 ~7µs（kdq r0 尾/G5 退队/kdq r1 填/strip/S(0) 执行——需 round ring 或 r1 预填，解冻 kdq 机器另批）+ grads 相位环 2×案 1 税闭环 + 案 1 调用级 3/4（结构性）。
[tail-recon-z0] 尾部残链压缩纸面侦察开工（只读轮，内核零字节）。范围：三候选路线（strip 预填提前 / kdq r1 预填 / S(0) 跨 bundle 提前发射）的 GO/NO-GO 裁决。基底 = HEAD 035ddf3（v9-LB + L2-lite，19.09ms）；trace 以 v9lb_r1_iket.json 近似判读（l2lite_r1 candidate 未见于 scratchpad）。
[tail-recon-final] 尾部残链压缩纸面侦察终裁（只读轮，内核零字节；v9lb_r1_iket.json CTA0 b=2..13 逐边界填图为证，l2lite_r1 trace 未达即用 v9-LB 近似——LOAD_K 段在链是唯一已知差异，判读时已剔除）：**路线1 strip 预填 = GO（R-低，先落）；路线3 S(0) 跨 bundle 提前 = GO（主奖，R-中，需路线1为前置）；路线2 kdq r1 预填 = NO-GO（字节+协议双死刑，且被路线3支配）。推荐序 1→3，2 弃案。**
  (0) trace 翻案两条（改写旧填图口径）：(a) "S(0) 执行 2.9µs" 实为 **leader 发射串行 ~2.0-2.4µs + TC 执行尾 ~0.3-0.6µs**（S_ISSUE(4b+4..5)+dP 尾实测 +6.9→+9.3，WAIT_S end +9.6：执行几乎全程躲在发射后面）——尾链是 leader 发射带宽问题，不是 TC 执行问题，与案1"调用级税 3/4"同源；(b) G5 波极快：g18 信用（G5 r0 读完成）在 r0 commit 后仅 ~0.5µs（+0.35→+0.90），G5 不是尾链的重头。v9-LB 实测全尾链（pair7 WAIT_dK end → grads(b+1) 首 dVdK_ISSUE）= **13.6-14.2µs**，构成：r0 尾 0.35 → [LOAD_K 3.3 在链，L2-lite 已摘] → r1 填+会合 →+5.6 → strip 0/1(b+1) →+7.1 → pass0(b+1) 发射 →+9.3 → S 退队 +9.6 → math T2R/softmax/publish 两 slice →+12.1 → **leader 被 pass1(b+1) 发射占用至 +13.5** → grads(b+1) +13.9。L2-lite 后推算残链 ~10.5µs（非旧口径 7µs：旧图漏了 math 双 slice 发布与 pass1 发射两段）。
  (1) 路线1（strip 预填提前，W17 指令流重排，L2-lite 同款）：**GO**。信用论证：strip gen0/1(b+1) 的 acquire 信用 = pass1(b) 的 stage 释放，实测边界前 ~27µs 即开（S_ISSUE(4b+2/3) 于 -30..-27 结束；MAT_ACQ(b+1,ord0/1) 驻留 0.03µs = 零等待铁证）；W17 空闲窗 = wide-end（边界-4.4..-6.1）→ g16 acquire park 2.6-3.1µs ≫ strip 双填 ~1.3µs，塞入后 r0 会合零延迟（余量 ~1.4µs）。手术区（全落 W17 角色体，协议零改）：strip 4-gen 块 14934-14998 拆分——gen{2,3}(b) 留迭代头、gen{0,1}(b+1) 提升至 wide 尾 commit（15105-15108）之后 / g16 acquire（15118）之前；序幕剥离 gen{0,1}(首bundle) + 末迭代剥离（无 hoist，静态纪律同 [l2l-z0]）；LOAD_QDO/MAT_ACQ payload 用 loop_iter+1。strip 管线全局 gen 序逐字不变（0,1,2,3 per bundle 同序，仅墙钟前移）→ leader/math/gather 零触碰。收益：独立落地 ~1.3-1.5µs(trace)/~1µs(release) per bundle（-2.5~3%）；真正价值 = 路线3的硬前置 + 硅上验证 W17 空闲窗假设。风险 R-低（机械重排+剥离，correctness 门必抓相位错）。
  (2) 路线2（kdq r1 预填）：**NO-GO，三重死刑**。(a) 字节：真解耦需 r1 双波与 r0 双波同时活 = 4 个 16,384B kdq 槽 = +32,768B > 净余 17,408（[l2-recon-final](1) 同账）；(b) 协议：round ring 2→N 在 2 物理缓冲下 WAR 保护即坏——acquire(g) 等 release(g-N)（N≠2）守护的不再是同一缓冲的上一占用者（buf(g%2) 的最近占用是 g-2），ring 3 直接竞态、非工程难度问题；真 ring-N 需 N 缓冲 + 静态 stage↔buffer 绑定，而 20 gens/bundle % 3 ≠ 0 还会跨 bundle 相位旋转、砸烂 constexpr TMA 目标绑定（t_dot_smem_a/t_qt_smem_b/round_kd[w] 全静态）；ring 4 = +32,768B 回到 (a)；(c) n=1 单波影子（+16,384B 恰入账）只解耦 r1-wave0，wave1 仍钉死 G5 r0(b) 读完成（g17 释放），上界 ~0.9µs，代价 = 新 1-stage 管线 + G5 r1 的 A-fragment 分裂 + 填充机双目标分裂（中高危）——且路线3 placement-B 把 r1 段（信用等待+填充 ~2.1µs）整体藏进 pass0(b+1) 发射窗，r1 已不在链。支配弃案，存档即可。
  (3) 路线3（S(0)(b+1) 跨 bundle 提前发射 = 软件流水第一块砖）：**GO，placement-B 形态**（插入点 = G5 r0 波循环与 G5 r1 波循环之间，非 grads 之前——TC 按发射序退队，放 grads 前会把 G5 r0 顶后 ~2µs 反噬 r1 信用链；放 r0/r1 之间则 pass0 执行恰好罩住 r1 填充泡）。就绪三门论证（全部 trace 背书）：K(b+1) kres commit 边界前 ~25µs（L2-lite 已兑现，1-stage wait 提前到 b 尾无相位改动——wait(b+1)/release(b+1) 世代配对不变）；strip 0/1(b+1) 由路线1保证边界前已 commit；S/dP TMEM 槽 WAR = s_done/dp_done stage0 acquire，math 释放实测边界前 ~28.8µs（T2R_S(8b+2) end）。math 侧零触碰即受益：WAIT_S 提前 ~5µs 清除，T2R+softmax 溢入尾窗，PDS_ACQ 的 dqb_free 等待（G5 r1 退队 ~+4）将首次显形 ~0.3-0.5µs（已计入收益）。预演时序（边界=0）：r0 填 →+0.4 → G5r0 发射/退队 →+0.9 → [pass0(b+1) 发射 +0.5→+2.5，执行尾 →+2.8，罩住 r1 填 +0.9→+3.0] → G5r1 发射 +3.1（延迟 ≤0.1）→ 退队 +3.6 → math publish 两 slice →+5.2 → leader pass1(b+1) 发射 +3.2→+5.7（gen2/3(b+1) 由 pass0 自身释放 +1.5/+2.8 供给，W17 于 r1 后填 →+3.5/+4.3）→ grads(b+1) ~+6.3。**全尾链 13.9 → ~6.5µs**：相对 L2-lite 基底净省 ~4µs(trace)/~3-3.5µs(release) per bundle ⇒ e2e 19.09 → **~17.2-17.6ms 带（-8~10%）**。手术区（leader 角色体单区 + 路线1）：bundle 循环 14681-14848 旋转——序幕剥离 [kres wait 14686 + pass0(0)（14694-14755 的 sub_tile=0 臂）]；稳态环 tile_count-1 体序 [pass1(b)+kres release（14721-14755）; grads batch1 14761-14779; G5r0 14791-14816; **hoist: kres wait(b+1)+pass0(b+1)（插 14816/14822 之间，issue_seq=loop_iter+1）**; G5r1 14822-14841; dqb_free 14846-14848]；末迭代剥离（无 hoist）。_issue_score_pass_v6（15311-15472）逐字复用；全局 pass 序 pass0(0),pass1(0),pass0(1),… 逐字不变 ⇒ strip/s_done/dp_done/kres 相位代数全程不变，唯一语义变化 = 发射墙钟。风险：R-中（leader 是协议枢纽，旋转+双剥离复制体走样风险 > L2-lite；6 条管线状态穿过旋转但全局序不变，correctness 门可裁）；R-perf 看点两个（kres wait 上链余量 ~20µs、dqb_free 显形等待），trace 钩子 = WAIT_S(8(b+1)) end 应从边界+7 收到 +3 以内、MATH_PDS_ACQ 出现 ~0.4µs 驻留、dVdK_ISSUE(16(b+1)) 应 ~+6.5。
  (4) 推荐落刀序：**R1（strip hoist，独立验收）→ R3（leader 旋转）**；两轮各带 correctness 4/4 + release + trace 三件套。R2 归档不再议。R1+R3 兑现后残链理论下界 ~6µs，构成转为 [r0 填 + math publish 串 + leader pass1 发射带宽]——彼时案1调用级税（发射单价）成为下一战役的同一敌人。
  本轮改动：内核零字节；本文件 2 条日志（z0+final）为唯一写入。
[tail-z0] R1+R3 落刀开工（协调者解除只读，范围=[tail-recon-final] 两案；R1 先落自证后再 R3）。铁约束复述（动刀前逐字锚定）：(i) 协议层零触碰——pipe_strip 创建@13294/strip_mbars@12124、pipe_round 创建@13302/round_mbars@12125、pipe_kres 创建@13273/kres_mbars@12121、s_done/dp_done/pds/dkv_done/dq_done/dqb_free 创建与 mbar 全数不动；(ii) helper 本体零触碰——_issue_score_pass_v6@15311（内含 strip consumer wait/release）、_issue_grads_pair_v51、_issue_dq_wave_v32、_gather_kdq_v8 逐字不变；(iii) 全局序不变量——strip gen 序 0,1,2,3/bundle、pass 序 pass0(b),pass1(b)、kres wait/release N=N 交替配对、round g0..g19/bundle FIFO、kdq 会合序 r0(b),r1(b) 全部逐字保序（仅墙钟前移）；(iv) gather/math/relay/reduce 角色体零 hunk；(v) SMEM/TMEM 零字节；(vi) 静态纪律——producer/consumer advance 不入运行时 if，越界形态一律剥离（序幕/末迭代，l2l 先例）；W17 体内 tile_index 经查为死变量（单赋值零消费），末迭代剥离体不复制并注记。手术区：R1 = W17 角色体 14910-15204（strip 4-gen 环拆 2+2、gen{0,1}(b+1) 提升至 wide 尾/g16 acquire 之间、序幕+末迭代剥离）；R3 = leader 14636-14871（bundle 环旋转 placement-B：序幕 [kres wait+pass0(0)]、稳态 [pass1(b);rel;grads0/1;G5r0;kres wait(b+1)+pass0(b+1);G5r1;free]、末迭代剥离无 hoist）。
[tail-z1] R1 落刀完成（W17 单区），逐区 py_compile OK。落点（术后行号）：序幕剥离 strip gen{0,1}(首bundle) @15123-15196（payload Int32(0) 形，逐字复制填充块）；稳态环 cutlass.range(tile_count-1) @15197；环内 strip 环收窄为 gen{2,3}（strip_rel+2 形）@15221-15227；hoist gen{0,1}(b+1) @15398-15472（payload (loop_iter+1) 形，插于 wide 尾 commit 与 g16 acquire 之间）；末迭代剥离 @15566-15863（无 hoist；死变量 tile_index 不复制并注记）。自证：剥离体 vs 稳态体归一化 diff = 0（289=289 行）；strip gen 全局序构造性等价 N=1/2/3。
[tail-z2] R3 落刀完成（leader 单区），py_compile OK。落点：旋转注记 @14681-14697；大 if @14698；序幕 [kres wait + pass0(0), issue_seq Int32(0)] @14699-14725；稳态环 range(tile_count-1) @14726：体序 [pass1(b); kres release; grads batch0; batch1; G5 r0; **hoist: kres wait(b+1) + pass0(b+1, issue_seq loop_iter+1) @14876-14969**; G5 r1; dqb_free]；末迭代剥离 @14970-15060（无 hoist，N=1 退化 = 序幕+剥离体 = 原单 bundle 流）；tail 块并入大 if @15061-15083（内容逐字，删独立 if）。自证：剥离体 vs 稳态体 code-only diff = 0（103=103 行）。
[tail-final] R1+R3 终报（py_compile 全过，不 commit、不投测试）：
  diff：+729/−68（16,737 → 17,430；hunk 全落 leader 14681-14872 与 W17 14911-15202 两区旧行号带 + 文件尾 ADDENDUM）。铁约束自证：git diff 对冻结位点（Pipeline*create/kres_mbars/strip_mbars/round_mbars/_issue_score_pass_v6 def/_issue_grads_pair_v51 def/_issue_dq_wave_v32 def/_gather_kdq_v8 def/SharedStorage/MemRange）**零命中**；gather/math/relay/reduce 角色体零 hunk；SMEM/TMEM 零字节。
  核验表（符号仿真 N=1/2/3/16 全过 + 复制体 diff）：
   (1) strip gen 全局序 (b,g)：新旧逐项相等（序幕 {0,1}(0) + 稳态 [{2,3}(i);{0,1}(i+1)] + 末迭代 {2,3}(N-1) ≡ 0,1,2,3/bundle）；acquire=commit=4N=leader 消费数。
   (2) wide gen 序 / kdq 会合序 r0(b),r1(b)：逐项相等（gather 侧零触碰，配对不变）。
   (3) kres 配对：W(0),R(0),W(1),R(1),…,W(N-1),R(N-1) 交替不变（wait 移入 b 尾、release 留 pass1(b+1) 后，1-stage 相位代数同代配对）。
   (4) pass 全局序 P(0,0),P(0,1),P(1,0),…：逐项相等（s_done/dp_done/strip 消费态穿旋转不变）。
   (5) grads/G5/dqb_free/round consumer 序：逐项相等；producer_tail 5 处全存。
   (6) 旋转性质断言：P(b+1,0) 先于 R1(b)（leader）、strip(i+1,0) 先于 kdq(i,0)（W17）——两处 hoist 语义到位。
   (7) 剥离体保真：R1 剥离 289 行归一化 diff=0；R3 剥离 103 行 code-only diff=0（唯一差异 loop_iter→last_iter + 缩进）。
   (8) N=1 退化：两区均为 序幕+剥离体 = 原流逐字（无稳态环）。
  死锁审计：hoist 的 kres wait(b+1) ← gather K(b+1) fill ← leader kres release(b)（迭代头已发）——无回边；hoist 的 pass0 strip 等待 ← W17 strips{0,1}(b+1)（其 acquire ← pass1(b) 释放，迭代头已发）——无回边；W17 strips 提升块 acquire 信用实测边界前 ~27µs 即开，g16/g17/g18/g19 会合序与 gather 逐字同序。风险登记：R-低 机械剥离/复制（已 diff=0 自证）；R-perf 三看点——kres wait 上链（余量 ~20µs）、strip 填充挤占 W17 park（余量 ~1.4µs）、dqb_free 显形 ~0.4µs（已入账）。
  验收钩子（待硬件）：correctness 4/4（全局序不变量的硅裁决）→ release 判决（预估 −3~3.5µs/bundle，19.09 → ~17.2-17.6ms 带）→ trace：WAIT_S(8(b+1)) end 边界 +7..+9.6 → ≤+3；dVdK_ISSUE(16(b+1)) ~+13.9 → ~+6.5；LOAD_QDO gen0/1 与 S_ISSUE(4b+0/1) 现身上一 bundle 尾（payload 集合零增删，L2-lite LOAD_K 先例）；MATH_PDS_ACQ 允许出现 ~0.4µs 驻留。
  本轮改动：dsa_bwd_sm100_2cta_v7.py 两区 + 尾 ADDENDUM；本文件 4 条日志（z0/z1/z2/final）。不 commit、不跑测试（纪律）。
[tail-r2-z0] 硬件判决登记 + 拆刀开工：tail_r1（R1+R3 组合）correctness 4/4、release 19.558 vs 19.093 = **+2.4% 回退**。协调者裁决撤 R3 保 R1（归因待下一轮硬件：R1 单独贡献未测）。手术：leader 区回滚至 035ddf3（=git HEAD）逐字形态；W17 区 R1 全保留；文件尾 ADDENDUM 改口 R1-only + R3 撤刀注记。
[tail-r2] R3 撤刀终报（拆刀完成，py_compile 全过，不 commit）：
  基线勘误：协调者已将 R1+R3 提交为 **f8209c9**（tail_r1 硬件构建），故 HEAD ≠ 035ddf3——首次回滚误用 HEAD 做参照成了空操作（leader 区自比自过、diff 零 hunk 暴露异常），当即按指令改用 **035ddf3** 逐字参照重做。锚点唯一性此轮已断言（MMA/LOAD elif 全文件各仅 1 处）。
  回滚核验表：
   (1) leader 区（elif MMA_WARP → elif LOAD_WARP）**293 行逐字 == 035ddf3**（列表相等断言）；tail-R3 标记零残留；_issue_score_pass_v6 调用点恢复为 sub_tile 环内单点。
   (2) W17 区 R1 全保留：hoist/序幕/末迭代剥离标记齐全；剥离体 289=289 行归一化 diff=0 复检过；op 计数 strip acquire/commit 4/4、round 10/12、kdq_barrier 8、producer_tail 2。
   (3) diff vs 035ddf3 = 5 hunk（W17 区 4 + 尾 ADDENDUM 1），leader 区零 hunk；diff vs HEAD(f8209c9) = +96/−305（= R3 净逆转 + ADDENDUM 改口）。
   (4) 尾 ADDENDUM 改口：R1 标注 LIVE、R3 标注 RETRACTED（含 f8209c9 台账与失败假设），文件不再记载不存在的代码。
   (5) py_compile OK（17,221 行）。
  R3 失败模式复盘（假设，归因待 R1-only 硬件轮；留给 placement-C）：placement-B 把 pass0(b+1) 的**阻塞等待**（strip stage-0/1 consumer_wait 在 _issue_score_pass_v6 体内 + 前置 kres wait）搬进了 leader 串行流的 G5 r0/r1 中段窗——侦察的"strips(b+1) 边界前已 commit"前提取自 v9-LB trace，而 L2-lite 政权下 W17/gather 相位已移（LOAD_K 挪进 mid-bundle 与 strip TMA 争带宽、wide 环节奏变化可使 W17 晚达 hoist 位点），一旦 strips 未就绪，leader 卡在 G5 中段直接堵 G5 r1 发射→ dqb_free → math publish 全链，比同一等待落在原 bundle-head 位置（彼时 r1 填充/strip 填充已自然流逝）严格更贵；次要嫌疑：hoist 的 kres wait(b+1) 在 K 填充受挤压时同型发作。placement-C 设计约束：hoist 必须挂**非阻塞就绪探测门**（_mbarrier_try_wait@3100 探 strip full 边），探测选择两个静态剥离体之一（advance 不入运行时分支的铁律不变）——"落刀前先探，不赌 trace 外推"。另登记：+2.4% 为组合数，R1 单独贡献未测——若 R1-only 轮也回退，W17 空闲窗前提整体死刑，R1 亦撤。
  本轮改动：dsa_bwd_sm100_2cta_v7.py（leader 区回滚 + ADDENDUM 改口）；本文件 2 条日志（r2-z0/r2）。不 commit（纪律）。
[tail-r2 归因终审·全撤] R1-only 硬件判决：correctness 4/4，release 19.517 ≥ 19.09 阈值 ⇒ **R1 亦撤**（W17 空闲窗前提死刑：L2-lite 政权下 LOAD_K 已占 mid-bundle，strip 预填与其互相踩踏）。内核 git checkout 035ddf3 逐字回滚（l2lite 终态），py_compile OK。尾部战役收官账：L2-lite 入账（19.09ms），R1/R3 双撤但买到三条设计律——(a) trace 外推跨政权失效（每刀后前提须重验）；(b) hoist 含阻塞等待必须挂非阻塞探测门（placement-C 约束，_mbarrier_try_wait 选静态剥离体）；(c) leader 发射带宽 = 尾链本质，纯重排不增带宽（增益须来自等待消除或带宽再分配）。当前最优 = **L2-lite 19.09ms**（累计 −46.4%）。
[v10-recon-z0] V10 双发射侦察心跳（只读轮，内核零字节）：log 147 行全读、HEAD a80a799 确认干净（l2lite 终态）。进度：裁决①已有硬结论——W18=RELAY_WARP（dq_b peer-push 引擎，lane0 单线程协议链：pds_ready wait → 8KB bulk DSM → dqb_mbars 双 arrive → landing wait，每 bundle 全程在役且在 G5 关键链上），W19=IDLE_WARP 全内核零角色分支（15206 relay 后直落 common tail），grads 面迁入首选 W19；裁决②进行中——管线组绑定已核（leader_group=CooperativeGroup(Thread,1) 纯计数不绑 warp 身份 @13228，round/pds consumer 与 dkv_done/dq_done/dqb_free producer 调用点已定位 leader 体 14609-14867）；③④⑤⑥未启（待查 tcgen05 commit 逐线程语义、DSL 管线实现、寄存器 48 配额块 @13556-13583 已读、l2lite trace 未启）。
[v10-recon-final] V10 双发射 warp 特化纸面侦察终裁（只读轮，内核零字节；证据 = HEAD a80a799 源码逐行 + DSL 管线/tcgen05 实现源（DKG CuTeDSL）+ v7_full_source.csv SASS 分段 + l2lite_r1_iket.json CTA0 b2-13 稳态填图）：**GO——但附一条硬前置：双 warp UMMA 入队微探针（TC 前端跨 warp 串行化 = 唯一收益归零险，纸面不可裁）。新 mbar 需求 = 0；协议层/SMEM/TMEM/全部 helper 零触碰。**
  (1) W18/W19 职责裁决：W18=RELAY_WARP@11716-11719，lane0 单线程协议链（pds_ready wait→8KB bulk DSM→dqb_mbars 双 arrive→landing wait→再 arrive @15232-15299）——每 bundle 全程在役且是 G5 门 (mb_dqb) 的供给者，塞入 grads 发射会把 round/pds/dkv 环长等待插进 dqb arming 链 = 毒化 G5，否决。W19=IDLE_WARP 全内核零角色分支（relay 后直落 common tail @15301-15308），零管线职责，已在 decrease(48) 桶（quota 块 @13556-13560 `warp_idx>=MMA_WARP`）——**迁入宿主定为 W19**。红利：w16%4=0 / w19%4=3 落不同 SMSP，发射 SASS 流真并行。
  (2) 协议属权迁移面（全清单，行号=现役）：迁 w19 = round_cons（建 @14609；G5 wait/rel @14796/14815、14827/14840；grads 内 @15592/15608、15616/15632）、pds_cons（建 @14617；双 wait @15574-15577、双 rel @15640-15643）、dkv_prod（建 @14621；acq/commit @15589/15610、15613/15634）、dq_done_prod（建 @14628；tile 头 acquire @14648；TAIL commit @14861）、dqb_free_prod（建 @14632；预 arm @14642-14644；每 bundle @14846-14848）、dqb 裸门 wait @14799-14802 + dqb_parity @14682、两个 grads 调用点 @14738/14762、G5 双环 @14791-14841、TAIL 三 producer_tail（dkv/dq_done/dqb_free @14865-14867）。留 w16 = s/dp_prod、strip_cons、kres_cons（@14589-14608）、kres wait/rel @14686/14731、双 pass 调用 @14697-14719、TAIL s/dp tail @14863-14864。mbar 计数/相位不变性：leader_group=CooperativeGroup(Thread,1)@13228 纯计数不绑 warp 身份；所有迁移 op 单线程执行（管线 arrive 内置 elect_one，helpers.py@312-335；wait 为相位轮询非消耗性）；w19 新建状态相位 0 起步与现役 w16 状态同构，产=耗账逐管线不变（round 20=20、pds 4=4、dkv 16=16、dq_done 1=1/tile、dqb_free N+1=N+1）。producer_tail 内置 is_leader_cta 门（sm100.py@691-698）；w19 分支体须整体套 is_leader_cta（现役 w16 同形）。
  (3) tcgen05 双发射合法性：**合法，且无需任何新栅栏**。(a) DSL cute.gemm 自动 elect_one（mma.py@616-625"do NOT wrap"），两 warp 各自持锁发射合法；(b) tcgen05.commit 按**发射线程自身流**追踪（DSL commit→nvvm.tcgen05_commit @tcgen05/helpers.py:186-234；全文件工作模型"tracked set is EXACTLY S(0)+S(1)"@14724-14731 仅在逐线程+程序序语义下成立，v5-v9 硅上背书）——52 个 UTCBAR 站经 NCU SASS 逐一归面：score 面 9（strip×4+s/dp×4+kres）、grads 面 36（round/dkv×32+pds×4）、G5 面 6（round×4+free+dq_done），**无一站需要跨面追踪集**，随 MMA 同 warp 迁移后每站追踪集恰缩为本面（kres 追踪集从"程序序恰好"升级为"构造性恰好"）；(c) TMEM 三面互斥（S/dP[0,128)/dkv[128,256)/dQ[256,512)）+ SMEM 操作数互斥（K驻留/strip ↔ slab/round/dq_b），跨面数据边全部经 AsyncThread 代理（math/reduce/W17/relay）现有 mbar+fence 链——不存在跨 warp UMMA→UMMA RAW，故无需 tcgen05.fence 对；(d) ACCUMULATE 经 mma.with_() warp 本地副本 ✓。**唯一纸面不可裁点：两发射线程对同一 TC 前端的入队吞吐是否串行（若串行且案1税为前端界，收益塌缩至等待重叠分量）——微探针（两 warp 各发不相交 TMEM 的 UMMA 流测入队速率 vs 单 warp）先行。**
  (4) 寄存器账（v7_full.ncu source CSV，UTCBAR 界标三分段）：leader 区 UMMA 128/64/16（score/grads/G5）；**spill 站 score 247（139 STL+108 LDL，134.5 op/bundle）vs grads 24（24.0/bundle）vs G5 10（8.1/bundle）**；动态指令 893/581/178 条/bundle；停顿采样 4955/15942/10495（grads/G5 段停顿=环等待，非算力）。判决：grads+G5 发射机器在**与 score 全机器共驻的 48 reg 下今天就近零溢出**（34 站/32 op/bundle），独占 w19 分支后只会更好——48 站得住，配额表零改；w16 甩掉 grads/G5 活值（5 管线状态+7 fragment+army/t_dq+parity 跨 score 区活）后 score 段 134.5 op/bundle 溢出应显著回落 = 未计入的正向红利（task 口径"185/150"系混段账，分段账以此为准）。
  (5) 跨面同步新增点：**零新 mbar，两条既有链已天然承载**。(a) grads 等 dS：pds 管线原样随迁（math 产 @14410 → w19 耗），math 等 S 经 s_done（w16 产）——两面经 math 解耦 ✓；(b) dkv 环信用：dkv_done 整体随迁 ✓；(c) 全案唯一隐性程序序依赖 = relay DSM 源 WAR（管线注 @13320-13323：math(b+1) 覆写 dS slab vs push(b) 读，现靠"G5(b) 先于 score(0)(b+1)"的 leader 串行序传递）——**拆分后仍闭合**：G5(b) MMA 完成 → w19 dqb_free(b) commit（tcgen05 完成追踪，强于发射序）→ math(b+1) slice-0 dqb_free wait @14269-14271 **先于首次 slab store @14294**（逐行核验），链条反而更强；(d) 结构性领先界：W17 单串行流（strips(b+1) 排在 wide(b)+kdq(b) 之后）天然把 score 面领先钳在 ~1 bundle——正是目标软件流水形态，无需管制。
  (6) 收益上界（l2lite trace b2-13 稳态，P=37.07µs）：现役串行环 = score 窗 6.7（发射实работ 5.9）+ grads 窗 23.9（发射仅 2.6，余 21 为 pds/环等待）+ 尾隙 6.2。拆分后两面并行，理论周期 = max(链)：reduce 忙链 16.8µs/warp（WAIT_dK 10.4 为环闲可压）、W17 供给链 ~16、math 忙 ~4（WAIT_S 19.4 全是槽位闲）、score 面 ~7 ⇒ **trace 制下界 ~20-24µs（−35~45%）**。折 release 制（现 ~21.6µs/bundle @19.09ms）：摘除 score 发射占环 ~4-5µs + 等待重叠 1-3µs ⇒ **−4~7µs/bundle ⇒ e2e ~13.7-15.5ms 带（−19~28%）**；退化情形（TC 前端串行化坐实）收益缩至等待重叠分量 −5~10%。对照三设计律：(a) 估算全取自 l2lite 政权 trace ✓；(b) 零阻塞等待跨流搬运（各面等待留在各自流内，探测门问题不存在）✓；(c) 双发射 = 真增发射带宽，是律 (c) 唯一许可的增益类 ✓。
  手术面分级：F0 前置微探针（独立小内核，半日）；F1 低危 = w19 分支体新建（grads/G5/free/dq_done 全机器平移 + is_leader_cta + 本地状态重建，helpers 逐字复用）+ w16 对应块删除；F2 低危 = TAIL 拆分（s/dp 留 w16，dkv/dq_done/dqb_free 随迁）；F3 低中危 = span 属 warp 迁移（dVdK_ISSUE/TAIL 换宿主，29/29 名集零增删；w19 的 ROLE mark 政策——建议复用 ROLE_MMA 保名集，协调者裁）；F4 零改 = 协议创建层/SMEM/TMEM/四 helper/gather/math/reduce/relay/W17。风险表：R1 TC 前端串行化（收益险，F0 裁决）；R2 w19 溢出超 48（NCU 反证基本排除）；R3 复制体走样（[l2l] 断言锚定纪律 + correctness 门）；R4 score 面自由跑的 s/dp 2-stage 节流形态属性能观察项非正确性险。
  本轮改动：内核零字节；本文件 2 条日志（z0 心跳 + 本终报）为唯一写入。
[v10-F0 判决·NO-GO] F0 探针（issuers=2 微基准，B200 实测）：双 warp 向不相交累加器并发入队，每总原子价 N64 +0.51%/N128 +0.31% ≈ 完全持平 ⇒ **TC 前端跨 warp 入队串行**，V10 双发射收益带（13.7-15.5ms）归零，全案关闭。探针成本半天，避免一场注定无效的协议属权大手术（侦察员的"先探后刀"纪律兑现）。杠杆队列更新：h128 单 stage 粗化（4 S_ISSUE→1、N128 原子 2.61×、放弃 bundle 内双缓冲赌 math slack 吸收）上位为主候选；V11（drain 解耦/原子占空比 43%→90%）侦察在飞。
[v11-recon-z1] 占空比实测核验心跳（只读轮，内核零字节；l2lite_r1_iket.json 全量解析，CTA0/CTA1 各 8 reduce 警 ×128 融合单元逐 span 填图）：**论题成立**——稳态 b2-13：周期 37.05µs/bundle（trace 制；release 制 ~21.6），REDUCE_ATOMIC 跨 8 警并集 17.18µs（CTA0）/18.91（CTA1）⇒ 占空比 **46.4%/51.1%**（协调者 43% 论题在 release 制口径 ✓）。但空洞解剖改写归因：死时间两分——(a) **边界洞 9.85µs**（bundle 头 WAIT_dK，= grads(b+1) pair0 的脊柱生产间隙）占 26.6%；(b) **环内洞 ~10µs = drain 自身串行开销**：单元节拍 3.42µs = T2R 发射 0.5 + T2R→ATOMIC 间隙 1.25（TMEM 数据延迟/spill 回读）+ 原子发射 1.71，环内 WAIT_dK 仅 **0.08µs≈0**——环内 MMA 恒领先 drain，drain 不饿；饿的只有边界。铁证对齐：leader 偶数块（blk4/6/8...）发射 gap 终点 == drain T2R end（环 2 信用 = T2R 释放，b5 逐块核对），leader 环等待合计 16.6µs/bundle（grads 窗 23.8 的主体）——**环 2 把 leader 锁在 drain 节拍上（方向与论题相反：不是 MMA 卡 drain，是 drain 卡 MMA），而边界洞与环深无关**。
[v11-recon-final] V11 drain/脊柱解耦纸面侦察终裁（只读轮，内核零字节；证据 = l2lite trace 全量填图 + HEAD a80a799 源码逐行）：**A = NO-GO（几何死刑）；B = NO-GO（字节+泵吞吐双死刑）；C1（drain 软件流水，自行增补）= GO 为 v11 主刀。**
  (1) 变体 A（dQ 半驻留换环深）NO-GO——**不存在数学成立的切法**。dQ 驻留需求 = [D512×h128] f32 累加器（DQ_MMA_TILER (256,128,64)@11899、DQ_D_ROUNDS=2/DQ_KV_WAVES=2，每 bundle 4 wave 双 round 全数落账），per-CTA = 128 lane×256 col，**尺寸由 D×h 积决定、与 kv 覆盖无关**。三切法逐一否决：(i) D 半（留 t_dq[0]）：r1 的每 bundle wave 积无处落——kdq 流每 bundle 双 round 都到，deferral = kv 二遍流 = 重放 gather/score/math 全脊柱，出变体范围；(ii) h 半（2 round×h64=128 col）：非驻留半每 bundle 仍需 [D256×h64]×2 transient 累加器 = **恰好 128 col，吃掉刚释放的全部列——净释放 = 0，命题循环自杀**；若非驻留半按 bundle 排空走原子流 = +128KB/bundle/cluster 灌进正在抢救的原子管（dKV 512KB 之上 +25%，drain 节拍 3.42→~4.3 反噬周期），半张量 16× 流量 + 复活 f32 dq_acc workspace（[z0](f) 刚退役）；(iii) kv 时分（"每 tile 排空 2 次"）：累加器尺寸与 kv 覆盖无关，两 round 仍须同驻 256 col，**一列不省**——任务书"2× dQ 流量"前提没有可成立的几何。即便 128 col 白来（假设免费 ring 4）：环 4 仅把 leader 末块信用从 u6-T2R 提前到 u5-T2R，grads 窗 23.8→~18.5（省 ~5.3µs），2 单元积压 ~6.8µs 垫边界洞 → 周期 ~30-31µs → e2e ~15.7ms——**与 C1（零 TMEM 零协议）同带，被支配**。归档不再议。
  (2) 变体 B（SMEM drain 中转站）NO-GO——字节死刑（任务书预设卡点坐实，先算先斩）：中转单元 = 融合后 f32 对像 = 32 f32/thread × 256 thread × 4B = **32,768B/CTA > 净余 17,408**（[l2-recon-final](1) 账；bf16 压缩中转 = 精度铁律禁）；半对 16,384 勉强入账但深度 = ½ 单元 ⇒ drain/泵锁步 = 零解耦纯加跳。泵吞吐第二死刑：4,096 原子/对/CTA ÷ 32 lane（W19 独警，v10 死后 W19 虽归还、全内核仍只此一家空闲）= 128 red/thread ≈ 8-13µs/对 ≫ 3.42 节拍，泵需 ≥4 警而无人可抽（抽 reduce 警 = 自噬）。第三刀：中转不造数据——9.85µs 边界洞分毫不动。归档。
  (3) 变体 C1（增补主案：drain 软件流水，intra-bundle）**GO**——把"解耦"从缓冲问题改判为**延迟隐藏问题**：单元 u 的原子发射（1.71µs，不触 TMEM/mbar）之下前置隐藏 u+1 的 [WAIT + T2R 发射 + TMEM 数据延迟]（合 ~1.75µs）。形态 = 半提升：fuse(u) 完成后 [探测 u+1 → T2R-issue dv(u+1)] → ATOMIC(u) → T2R dk(u+1) → fence → 双 release → fuse(u+1)。寄存器账：半提升峰值 = fused(u)32 + dv(u+1)32 = **64 值寄存器 == 现役峰值**（reduce 128 配额 + v11 注记 23 残余 spill 不恶化；全提升 96 值 + 解码临时贴 128 顶，弃）；协议账：wait/release 计数与世代配对逐代不变、release 仍在 fence 后（TMEM 读毕才放）——**纯墙钟重排，协议层/SMEM/TMEM/mbar/其余全角色零字节**。铁约束两条：末单元 u=7 不提升（constexpr 环静态剥离——跨界提升会把 10µs 边界阻塞拖到 u7 原子之前，l2l 剥离纪律）；u4（pair1 首单元，blk8/9 需 pds2/3）的前置 wait 在加速节拍下可能撞 math 发布 ⇒ **tail-r2 设计律 (b) 强制：挂 _mbarrier_try_wait 探测门（@3100 先例）选两个静态体之一，不赌**。收益模型：节拍 3.42→~2.2-2.6，环 2 信用随节拍加速 → leader 偶数块等待 16.6 同步收缩 → grads 窗 23.8→~17 → 周期 37→~30-32 → **e2e 19.09 → ~15.5-16.5ms（−13~19%）**，占空比 46→~57-62%；退化护栏：若 1.25µs 间隙主体为 spill-LDL 与 red 抢 LSU 端口（v11 注记六个 64-bit 地址临时量 spill 在案），收益打折至 ~−8%。
  (4) 手术面分级（C1）：F1 中危 = _drain_dkv_fused_v52（v5.2 冻结 helper @15693）解冻拆 [wait+T2R+fuse]/[atomic] 两段 + reduce 角色内环重构（@14546-14578；解码/原子体逐字复制断言锚定，try-wait 门 + 末单元剥离）——**全案唯一手术区**；F2 零改 = 协议创建层/SMEM/TMEM/leader/math/W17/gather/relay 全平面；span：WAIT_dK/REDUCE_T2R/REDUCE_ATOMIC 三名保留、payload 不变（跨单元墙钟交叠属语义注记入尾注），29/29 名集零增删。
  (5) 推荐与格局：**C1 单独成刀 = v11 本体；A/B 归档**。边界洞 9.85µs 的真解药在生产侧——v10 死后唯余 h128 单 stage 粗化（压 score 窗，杠杆队列现任主候选），与 C1 完全正交（C1 只动 reduce 警）可叠加：双兑现账面周期 → ~25-27µs、占空比 → ~65-70%；占空比 →90% 终局还需边界洞归零 = 生产侧软件流水（S(0) 跨 bundle 类，placement-C 探测门约束下重启）= 另批 spec。全部收益数为 trace 制（37.05µs/bundle），e2e 带已按 release 21.6/37 折算。
  本轮改动：内核零字节；本文件 2 条日志（z1 心跳 + 本终报）为唯一写入。
[v11-C1-r1 判决·回滚] correctness 4/4（序恒等/协议守恒/探测门语义全部硅裁决通过——实现无瑕）；release 20.419ms vs 19.09 = **+7.0% 回退** ⇒ e2e 铁律回滚（revert 8d176d6）。
  归因：预取拉长 dV 片段活期（fused(u) 与 dv(u+1) 同时活），侦察的"峰值 64 值 == 现役"只计值寄存器、未计原子段解码临时量（六个 64-bit 地址量本已在溢出边缘）的重叠 ⇒ ptxas 在 128 配额下多溢出，抵消并反超延迟隐藏收益。
  **战役级规律（第二次显形，与 v8-LE 同源）：本内核中一切"延迟隐藏"都以寄存器活期为代价，而寄存器是最紧资源——延迟隐藏类优化在此架构下系统性亏本。** 已证伪清单同族：v8-LE（math T2R 流水，spill −94% 仍 +1.3%）、C1（drain 流水，+7.0%）；反例（成功者）全部是"减少工作量/搬移位置"类：v7 驱逐回滚（减流量）、v9-LB（减窗跃迁）、L2-lite（挪指令位置，零新活期）。
  推论：后续杠杆应只在"减工作量/挪位置"象限内选择，"藏延迟"象限封盘。h128 粗化属前者（减发射条数+减协议开销，不新增活期）——杠杆队列现任唯一在案主候选。
[v9.2-LB2 落刀·协调者本人] K 向再肥化一档 D128→D256（获胜象限：减窗跃迁/零新活期，C1 教训后的选刀准则）。
  发现纠偏（重要）：v9-LB 并未减少 cute.gemm 调用数——k_blocks = size(k_fragment, mode=[2]) 随 K_CHUNK 同步翻倍，调用数恒 64/bundle；**它真正减半的是描述符窗跃迁（8→4），却换来 −4.1% e2e ⇒ 窗跃迁即窗级税的物理载体**（案 1 那 1/4 的实体）。本轮同一口井再打一档：窗跃迁 4→2。
  改动：SCORE_MMA_TILER (128,64,128)→(128,64,256)、SCORE_D_PIECES 4→2、K_CHUNK 128→256、K_CHUNKS/SCORE_A/B_STAGES 4→2、kres_rows 元组 4→2。字节恒等式：D256 下 **score-B 一级 == strip 一级**（STATIONARY_TILE_D 恰为 256），s = d 平凡成立，比 v9-LB 的 s=4d+kb 更强；SMEM/TMEM/协议/其余角色零字节。早释放边界 SCORE_D_PIECES//2 = 1（piece0 用 stage0、piece1 用 stage1，1:1 对齐）。py_compile OK。
  判读：e2e 预期 −2~4%（19.09 → ~18.3-18.7ms）；若零收益则窗级税在 D128 已饱和（同样是有价值的边界数据，回滚零成本）。
